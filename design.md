# Home Purrtect — UI 디자인 시스템 (design.md)
> **이 문서의 역할**: 바이브 코딩 시 UI가 매번 달라지고 평범해지는 걸 막는 **디자인 규칙(토큰)**. Code는 UI를 만들거나 손볼 때마다 *먼저 이 문서를 읽고* 아래 토큰·규칙을 따른다. "예쁘게"가 아니라 "이 토큰으로".
> **톤 한 줄**: **고전 카툰(Popeye·Cuphead·Tom&Jerry·Felix의 1930s~50s 인쇄물)**. 두꺼운 잉크 외곽선 + 좁고 강한 팔레트 + 빈티지 인쇄 질감 + 굵은 칸 분할.
> **레퍼런스 보드**(작성자 선정): ① **톤·아트** = Tom&Jerry·Bugs·Cuphead Funnies·Popeye·Felix·Zizone(1930s~레트로팝 인쇄물). ② **GUI 규율** = 쿠키런(부품 일관성·정렬·여백). ③ **톤+규율 공존 증명** = **ACECRAFT**(우리와 똑같은 컵헤드 무광 카툰 톤인데 모바일 상업 게임 수준 GUI 규율 — "톤은 컵헤드, 완성도는 모바일 표준"이 실제로 된다는 산 증거). → 공통 DNA를 아래에 토큰화.
> ⚠️ **ACECRAFT는 세로 비행슈팅** → *전투 HUD 배치는 참고 금지*(방향 다름). 로비·캐릭터선택·업그레이드·보상·가챠 같은 **메타 UI 규율만** 가져옴.
> **기존 자산**: STATUS의 "Caldera 디자인 시스템"(Digital Orange CTA·둥근 알약 버튼)을 *대체가 아니라 정식 확장*. (폰트는 DoHyeon 전량 폐기 → Pretendard[본문]+잘난체/Luckiest Guy[타이틀]로 교체, §2.)
> **태그**: `[확정]` 그대로 / `[열어둠]` 취향 결정 필요.
> **⚠️ 검증 상태(정직하게)**: 색 hex·글자효과 px·간격은 *아직 실기기/실코드로 1회전 안 돌린 "시작값"*. **§9 토큰을 코드로 박고 → 버튼 1개·카드 1개를 실제로 화면에 띄워 → 눈으로 보고 교정**하는 1회전을 거쳐야 비로소 진짜 [확정]. 특히 크림↔골든 명도 근접(§1·§14)·title 외곽선 두께(§2.2)는 교정 1순위.
> **SSOT(단일 출처) 규칙**: 값의 *주인은 §9 토큰*이다. §1·§2·§3.5 등에 같은 수치가 적힌 건 *설명용 사본*일 뿐 — 값을 바꿀 땐 **§9를 고치고**, 다른 섹션 수치는 거기 따른다(충돌 시 §9 우선).
> **구현 메모(Code)**: §9 토큰 오토로드의 실제 이름은 **`Design`**(파일 `scripts/style.gd`). 모든 스크립트가 `Design.*`로 참조. 버튼/패널은 `Design.style_button()`·`Design.make_panel()` 헬퍼로 베벨·물건화 자동 적용.
---
## 0. 핵심 원칙 8 (이걸 어기면 톤이 깨짐) `[확정]`
> 레퍼런스 11장 + ACECRAFT 5장에서 *예외 없이* 공통으로 나온 것 = 이 게임 UI의 헌법.
1. **모든 요소에 두꺼운 잉크 외곽선 + 단순 면.** 버튼·패널·아이콘·HP바·캐릭터 전부 **검정 두꺼운 테두리**. 그라데이션·사실적 음영·반투명 글래스 전부 금지(전 레퍼런스에 0). 면은 색으로 딱딱 나눔. → "구글 머티리얼/부트스트랩 느낌" 나오면 외곽선부터 의심. *(외곽선은 순흑보다 **따뜻한 먹갈색 #241F1B**이 ACECRAFT 톤에 더 맞음.)*
2. **좁고 강한 팔레트.** 한 화면에 4~6색. 바탕=크림, 구조=검정 잉크, 주인공/강조=골든·빨강. 그 외 색은 *의미 있을 때만*(파랑=물·밤, 초록=해금 등). Popeye 포스터가 빨강+크림+검정 3색으로 제일 강렬한 걸 기억.
3. **빈티지 인쇄 질감 한 겹 — 캐릭터뿐 아니라 *UI 패널·버튼에도*.** *이게 "고급 카툰"과 "값싼 클립아트"를 가르는 핵심.* 깨끗한 벡터면 디지털 티 → 살짝 바랜 종이결 + 미세 망점(하프톤) + 1~2px 인쇄 어긋남 느낌을 전체에 깔기. **ACECRAFT 교훈: 버튼·패널까지 질감을 얹어야 "UI만 매끈한 디지털"로 안 떠 보인다.** (코드: 오버레이 텍스처 1장 + 살짝 낮춘 채도.)
4. **굵은 칸(그리드) 분할.** 정보가 많은 화면(상점·도감·결과·도전과제)은 Tom&Jerry/Zizone처럼 **굵은 테두리 칸으로 분할**. 칸마다 한 가지 정보. 떠다니는 카드보다 *짜인 칸*이 이 톤.
5. **대칭 + 만화기호 + 여백 장식.** 패널·타이틀 좌우 대칭, **빈 곳엔 별·X·전구 줄·삼각 깃발·집중선·폭발을 *장식*으로** 흩뿌림(과하지 않게). 1930s 특유의 "여백을 기호로 채움". **ACECRAFT 교훈: 이 앰비언트 장식이 코드 도형만으로도 즉시 톤을 살린다(가성비 최고).** 휑한 여백 = 와이어프레임 느낌의 주범.
6. **여백은 8의 배수.** 모든 간격·크기를 8px 그리드(8/16/24/32…). 들쭉날쭉 간격 = 아마추어 느낌 1순위 원인.
7. **★패널은 "물건"이다 (프레임 물건화 + 하드웨어) — 고급감의 70%.** *이게 ACECRAFT를 "프리미엄"으로 만드는 진짜 정체.* 패널을 *평면 둥근 사각*으로 두지 말고 **나무 간판·티켓·양피지·코르크보드·청사진 같은 "물건"**으로. 거기에 **하드웨어**(볼트·나사·리벳·로프·경첩·테이프·티켓 절취선 점선)를 박는다. 질감(원칙3=표면)과 물건화(형태+하드웨어)는 *다른 축* — 둘 다 필요. 구현: 9-slice 프레임 텍스처가 정석, *임시는 코드로* (이중 테두리 + 모서리 볼트 4개 도형 + 제목 리본). → 화면 종류별 물건 매핑은 §3 참조.
8. **★버튼·요소는 베벨 입체 (플랫 금지).** 평평한 색면 + 얇은 띠그림자로 끝내지 말 것. **위쪽 하이라이트 + 아래쪽 짙은 면 + 두꺼운 외곽 + (선택)볼트** = 나무/캔디 같은 *물리적 입체.* 단색 그림자(원칙 유지)에 *상단 1~2px 밝은 라인*을 더하면 즉시 베벨. → §3 버튼 참조.
---
## 1. 컬러 토큰 ★치즈 = 브랜드색 `[확정 / hex는 시작값, 실기기 튜닝]`
> **결정: 메인 = 치즈 골든, 포인트 = 빨강.** 주인공 이름이 "치즈"라 *캐릭터 색이 곧 브랜드 색.* 골든+빨강+크림+검정 = Popeye·Cuphead·Tom&Jerry가 다 쓰는 검증된 고전 카툰 공식.
> Code: 이 값들을 **§9 토큰 한 곳**에 정의하고, 어디서도 색을 직접 입력하지 말 것(토큰 참조만).
### 바탕 / 구조 (화면의 90%)
| 토큰 | hex | 용도 |
|---|---|---|
| `ink` | `#241F1B` | 모든 외곽선·본문 텍스트(따뜻한 먹색) |
| `paper` | `#F3E3BE` | 패널·카드 기본 배경(바랜 종이) |
| `paper-deep` | `#E4CB95` | 카드 안쪽 음영·비활성·칸 구분 |
| `canvas` | `#2A211C` | 화면 바탕(어두운 우드톤) |
### 브랜드 / 강조 (귀하게)
| 토큰 | hex | 용도 |
|---|---|---|
| `cheese` ★메인 | `#F2B33D` | 치즈·로고·**긍정 CTA(레벨업·구매·해제·보상받기)**·동전·별·Lv5 |
| `cheese-deep` | `#D4912A` | 골든 음영·눌림 |
| `red` ★포인트 | `#D6402E` | **전투 CTA(출격·전투시작·보스도전)**·HP바·위험. 화면당 1개 |
| `red-deep` | `#A82B1E` | 빨강 눌림 |
| `ink-cream` | `#F3E3BE` | 잉크 띠·빨강 버튼 위 글자(역상) |
> **★CTA 색 = 의미로 분리 (ACECRAFT 증거):** **골드=얻는다(성장·획득·수락: 레벨업·구매·해제·보상받기) / 빨강=싸운다(전투·돌입: 출격·전투시작·보스도전) / 크림=뒤로(닫기·취소).**
### 보조 (의미 있을 때만)
| 토큰 | hex | 용도 |
|---|---|---|
| `teal` | `#2E8B8B` | 밤·물·차분한 보조 |
| `blue` | `#3E6FB0` | 정보·세이브·하늘 |
| `green` | `#5E8C4E` | 해금·완료 체크 |
| `pink` | `#E8729C` | 펄·하트·로맨스 한정 |
### 등급 라벨 색 `[확정]`
| 등급 | 토큰 | hex |
|---|---|---|
| Lv1 이름 없는 | `rank-1` | `#9A8C7A` |
| Lv2 떠오르는 | `rank-2` | `#5E8C4E` |
| Lv3 소문난 | `rank-3` | `#3E6FB0` |
| Lv4 이름난 | `rank-4` | `#8A5BA6` |
| Lv5 전설의 | `rank-5` | `#F2B33D` (=cheese) |
> 보석 5종: 조약돌=rank-1 / 자수정=rank-4 / 사파이어=rank-3 / 루비=red / 다이아=흰+blue.
---
## 2. 타이포그래피 `[확정]`
- **본문·UI·숫자 = Pretendard** `[적용됨]` — 가독성·글로벌. 본문은 절대 디스플레이 폰트 금지.
- **한글 타이틀 = 잘난체(Jalnan2)** `[확정]` — 제목·보스명·"클리어"·로고. 상업 무료.
- **영문 타이틀 = Luckiest Guy** `[확정]` — "HOME PURRTECT"·"CLEAR!".
- ⚠️ **로고/메인 타이틀은 "폰트"가 아니라 "그래픽"**(글자 효과 §2.2).
### 2.1 타입 스케일 (1560×720 UI 좌표 px)
| 토큰 | px | 폰트 | 용도 |
|---|---|---|---|
| `fs-display-l` | 64 | 잘난체/Luckiest Guy | 시작 타이틀·보스명·게임오버 |
| `fs-display-s` | 48 | 잘난체/Luckiest Guy | "클리어!"·막 전환·큰 보상 |
| `fs-title` | 32 | 잘난체/Pretendard 800 | 패널 제목·버튼·NPC명 |
| `fs-body` | 22 | Pretendard 500 | 본문·대사·가격 |
| `fs-caption` | 18 | Pretendard 700 | 라벨·개수(720 최저선) |
| `fs-num` | 28~32 | Pretendard 800 | HP·동전·웨이브(외곽선 필수) |
- 위계는 이 6개만. 720 최저 18px. 버튼 높이 ≥ 60(터치). 위계 = 크기+굵기+색+효과.
### 2.2 글자 효과 — 위계 높을수록 두껍게
| 위계 | 외곽선 | 그림자 | 추가 |
|---|---|---|---|
| fs-display | 잉크 6~8px(겹선 OK) | 단색 잉크 offset 6~8 | 아치/3~6° 기울임 + bob |
| fs-display-s | 4~5px | offset 4~5 | scale bounce |
| fs-title | 2~3px | offset 2 또는 생략 | — |
| fs-body | 0~1px | 없음 | — |
| fs-caption | 0 | 없음 | 색 대비로만 |
| fs-num | 2~3px | offset 2 | 카운트업 팝 |
- 외곽선 두께 ∝ 글자 크기. 그림자 blur0·단색·한방향. 휨은 display만. 숫자는 항상 2~3px.
---
## 3. 컴포넌트 규칙 `[확정]`
### 버튼 — 완전 시스템
**원칙: 위계는 "색·채움"으로만. 크기·모양·외곽선은 전 버튼 공통**(§3.5: 높이 52, 알약=높이÷2, 잉크 외곽선 3~4px, 단색 잉크 그림자 offset 6).
**★베벨 입체(전 버튼 공통, §0-8):** ① 채움색 + ② 상단 안쪽 밝은 하이라이트 + ③ 하단 안쪽 짙은 면 + ④ 두꺼운 잉크 외곽선 + ⑤ 아래 단색 잉크 그림자. 강조엔 모서리 볼트. 눌리면 그림자 자리로 쑥.
**버튼 위계 5종 — 색=의미(골드=얻는다/빨강=싸운다/크림=뒤로):**
| 위계 | 채움 | 글자 | 언제 | 화면당 |
|---|---|---|---|---|
| Brand(골드 베벨) | `cheese` | 잉크 | 레벨업·해제·구매·제작·보상받기·수락·펄 | 보통 1 |
| Primary(빨강) | `red` | `ink-cream` | 출격·전투시작·다음막·보스도전 | 전투화면 1 |
| Secondary | `paper` | 잉크 | 닫기·취소·뒤로·일반 선택·탭 | 여러 개 OK |
| Danger | `paper` | `red` 글자/테두리 | 포기·삭제(빨강 채움 아님) | 필요시 |
| Icon/Close | 원형 `paper`/`teal` | 잉크 실루엣 | 닫기·설정(i). X는 빨강 허용 | 모서리 |
- **🚫 고스트(투명) 버튼 금지.** 보조도 크림 채움. "덜 강조"는 크기↓/모서리로.
- 강조 버튼 화면당 1개. 같은 색 강조 2개 금지. 취소=Danger(크림+빨강글자). 비활성=탁한 단색(paper-deep)+🔒, 반투명 금지.
### 패널/카드 — ★물건화(§0-7)
- **평면 둥근사각 금지. 패널="물건".** 크림 바탕 + 먹갈 외곽선 4~6px + **하드웨어**(모서리 볼트4 + 이중 테두리 + 맥락별 로프·테이프·절취선). 임시는 코드 도형.
- 화면별 물건: 상점=나무간판 / 보상=티켓·폴라로이드 / 이벤트=코르크보드 / 개조=양피지·청사진 / 결과=현수막 / 설정=금속판.
- 상단 타이틀 띠 = (기본) 잉크 띠+크림 역상 / (강조) 빨간 리본 헤더(중요 패널만, 남발 금지).
- **등급 테두리 색(§3 그리드)**: 카드 외곽선=등급색, Lv5=골든+광.
### HP바 / 아이콘
- HP = red 채움 + 외곽선 + 숫자 병기. 아이콘 = 단색 실루엣 + 동일 굵기 잉크 외곽선(동전=골든).
---
## 3.5 GUI 완성도 규격 ★쿠키런 실측 `[확정]`
> 정돈됨의 80% = 모든 화면이 같은 규격 부품 재사용.
### 핵심 원리 6
1. 헐벗은 요소 금지 = 모든 것이 그릇(캡슐·타일·카드) 안에.
2. 가로 바(버튼·캡슐·탭)는 전부 같은 높이(`BAR_H` 52).
3. 라운드 = 알약(높이÷2) 또는 사각(`RADIUS_CARD` 20) 둘만.
4. 아이콘이 그릇의 75~80% 채움(작게 띄엄 금지).
5. CTA = 하단 중앙, 제일 큰 버튼, 정해진 색·정위치.
6. 상단 재화 = 우측, 캡슐 등간격.
### 간격/크기 토큰 (720 px)
- 간격: `GAP_XS`8 / `GAP`16 / `GAP_LG`24 / `GAP_XL`32 / `EDGE`24.
- 버튼·캡슐·탭 높이 `BAR_H`52, 라운드=26(=÷2), 안 텍스트=32(높이의 62%), 좌우패딩 24+.
- 카드행 `CARD_H`88·라운드20·외곽선4·패딩16·아이콘=높이80%. 타일 `TILE`104 정사각·아이콘72%.
- 비율: 버튼=텍스트×1.6, 모든 가로바 52 한 높이, 아이콘=그릇75~80%, 라운드 2종만.
---
## 3.6 추가 컴포넌트 (즉흥 제작 금지) `[확정]`
- 탭바: 가로 바 `BAR_H`. 선택=PAPER+잉크외곽+아래패널과 연결. 비선택=PAPER_DEEP.
- 토글: 알약 트랙(32)+노브, ON=GREEN. 체크박스: 사각24·GREEN✓. 슬라이더: 트랙12·채움 CHEESE·노브32.
- 진행바: 높이24·맥락색(경험치 BLUE/제작 CHEESE/호감도 PINK/HP RED).
- 토스트: 상단중앙 아래로 탁(220ms)·1.5s 자동소멸·PAPER 알약. 툴팁: PAPER 말풍선+꼬리.
- 빈 상태: 시무룩 치즈 + 안내 + 유도버튼(휑한 화면 금지). 로딩: 치즈 루프/아이리스.
- 공통 상태: Selected=CHEESE 외곽+광, Disabled=PAPER_DEEP+muted(반투명 금지), Locked=어둡게+🔒+조건.
---
## 4. 레이아웃 / 안전영역 (가로 iOS) `[확정]`
- "16:9 세이프존 + 19.5:9 풀블리드": 핵심 UI는 16:9 안, 배경만 풀블리드.
- 디자인 캔버스 852×393pt. 양옆 ~44pt safe-area-inset 비움.
- HUD: 상단 얇은 띠(HP·웨이브·일시정지) / 하단 조작 행(이동·소모품·스킬·동료·근접·원거리). 16:9+44pt 안.
- 8px 그리드 / 터치 ≥44pt.
---
## 5. 연출 / 모션 — 지속·이징 명시 `[확정]`
| 모션 | 지속 | 이징 | 비고 |
|---|---|---|---|
| 버튼 등장 | 180ms | OUT+BACK | scale 0.8→1.06→1.0 |
| 버튼 눌림 | 70ms | OUT+QUAD | y+SHADOW_OFF, 그림자 소멸 |
| 패널 열림 | 220ms | OUT+BACK | 아래서 +24px+페이드 |
| 숫자 카운트 | 400ms | OUT+CUBIC | 끝에 1.15 팝 |
| 데미지 팝업 | 500ms | 떠오르며 페이드 | display |
| 화면 전환(아이리스) | 350/350 | IN_OUT+QUAD | 검정 원형 조리개 |
| idle bob | 1.6s 루프 | IN_OUT+SINE | ±3px |
| 등급/획득 강조 | 300ms | 별·반짝+scale팝 | Lv5·희귀 |
- 큰 모션 한 번에 하나. 반복은 미세하게. 선형 페이드만/블러 금지(BACK 탄성).
---
## 6. 사용법 — Code에게 거는 법 `[확정]`
> "design.md를 먼저 읽고 토큰(§9 `Design`)만 참조해 [화면]을 만들어/고쳐줘. 본문=Pretendard·타이틀만 잘난체/Luckiest Guy. 8px 그리드·먹갈 외곽선·**CTA는 골드(성장)/빨강(전투) 의미 분리·강조 화면당 1개**·버튼 베벨·패널 물건화(볼트·리본)·글자효과 §2.2."
- 고칠 때: "이 화면이 design.md 어떤 규칙을 어기는지 먼저 짚고 그것만 고쳐줘."
- 토큰은 §9 한 곳. 인라인 색·매직넘버 금지.
---
## 7. 빠른 체크리스트 `[확정]`
- [ ] CTA 의미대로(골드=성장/빨강=전투/크림=뒤로)? 강조 화면당 1개?
- [ ] 버튼 베벨 입체(§0-8)? 패널 "물건"(볼트·리본, §0-7)?
- [ ] 여백 장식 있나? 자원·메뉴 아이콘+숫자? 고스트버튼 없나?
- [ ] 취소=크림+빨강글자(빨강 채움 아님)? 비활성=탁한 단색?
- [ ] 카드 외곽선=등급색? UI 패널·버튼에 빈티지 질감?
- [ ] 가로바 전부 52px? 라운드 알약/사각 2종? 아이콘 75~80%? 헐벗은 요소 없나?
- [ ] 본문 Pretendard·타이틀만 디스플레이? 글자 외곽선 위계대로?
- [ ] 그림자 단색·블러0·한방향? 모든 요소 먹갈 외곽선?
- [ ] 골든을 강조·동전·긍정CTA에만? 색 토큰 안에서만?
- [ ] 간격 8배수? 터치 44pt+? 등장·눌림 모션(§5 ms·이징)?
- [ ] 등급을 색+별 개수 병행(색맹)? 골드 버튼 글자=잉크?
- [ ] 모든 수치 §9 `Design` 상수에서(인라인 금지)?
---
## 9. 복붙용 토큰 — GDScript 상수 (★SSOT) `[확정]`
> Godot이므로 GDScript 상수/Theme로 박는다. 오토로드 `Design`(`scripts/style.gd`). 모든 씬이 이 상수만 참조. 인라인 `Color("#...")`·매직넘버 금지.
```gdscript
# scripts/style.gd — AutoLoad: Design (SSOT)
extends Node
# 색(§1)
const INK := Color("#241F1B"); const PAPER := Color("#F3E3BE")
const PAPER_DEEP := Color("#E4CB95"); const CANVAS := Color("#2A211C")
const CHEESE := Color("#F2B33D"); const CHEESE_DEEP := Color("#D4912A")
const RED := Color("#D6402E"); const RED_DEEP := Color("#A82B1E")
const INK_CREAM := Color("#F3E3BE")
const TEAL := Color("#2E8B8B"); const BLUE := Color("#3E6FB0")
const GREEN := Color("#5E8C4E"); const PINK := Color("#E8729C")
const RANK := [INK, Color("#9A8C7A"), Color("#5E8C4E"), Color("#3E6FB0"), Color("#8A5BA6"), Color("#F2B33D")]  # 1~5
# 간격(§3.5)
const GAP_XS := 8; const GAP := 16; const GAP_LG := 24; const GAP_XL := 32; const EDGE := 24
# 크기(§3.5)
const BAR_H := 52; const BTN_MIN_H := 60; const TILE := 104; const CARD_H := 88
const ICON_IN_BAR := 36; const RADIUS_CARD := 20; const OUTLINE := 4; const SHADOW_OFF := 6
# 타이포(§2)
const FS_DISPLAY_L := 64; const FS_DISPLAY_S := 48; const FS_TITLE := 32
const FS_BODY := 22; const FS_CAPTION := 18; const FS_NUM := 30
const STROKE := {"display":7,"display_s":4,"title":3,"body":1,"caption":0,"num":3}
const MOTION := {"btn_in":0.18,"btn_press":0.07,"panel_in":0.22,"panel_out":0.15,"count":0.40,"dmg_pop":0.50,"iris":0.35,"bob":1.6,"rare_pop":0.30}
# 헬퍼: label(text,kind,color) / style_button(b,kind) / make_panel(...) / 베벨·물건화 자동
```
> Theme 병행: 기본 스타일박스는 `caldera_theme.tres`. 새 값은 §9에 먼저 추가 후 참조.
---
## 10. Do / Don't `[확정]`
### ✅ DO
- 모든 색·간격·크기는 §9 `Design` 상수에서만. 모든 요소에 잉크 외곽선. 가로바 전부 `BAR_H`. 라운드 알약/사각 2종. 아이콘 75~80%.
- CTA 색=의미(골드 성장/빨강 전투/크림 뒤로), 강조 화면당 1개, 하단 중앙. 버튼 베벨(§0-8). 패널 물건화(§0-7).
- 여백 장식. 자원=아이콘+숫자. 글자효과 §2.2(`STROKE`). 그림자 단색·blur0·한방향. 본문 Pretendard·타이틀만 디스플레이. UI에도 빈티지 질감. 간격 8/16/24/32만.
### 🚫 DON'T
- 그라데이션 채움(베벨 명암 예외)/블러 그림자/글래스/고스트 버튼/외곽선 없는 플랫. 빨강을 전투·HP·위험 외에. 취소 빨강 채움. 골든 큰 바탕. 같은 색 강조 2개. 버튼 플랫. 패널 맨 둥근사각.
- §2 6단계 외 폰트 크기. 비활성 반투명. 본문 두꺼운 외곽선. 토큰 없이 즉흥 색/폰트/라운드. DoHyeon 부활.
---
## 12. Surfaces — 깊이 위계 `[확정]`
| 레벨 | 값 | 용도 |
|---|---|---|
| 0 Canvas | `CANVAS` 또는 배경 아트 | 최하단 |
| 1 Panel | `PAPER` | 카드·패널·결과창 |
| 2 Inset/Modal | `PAPER_DEEP` | 안쪽 칸·비활성 / 모달은 INK 60% 딤 후 PAPER |
- 모달 딤 = INK 60%(블러 X). 레벨↑ = 외곽선 유지·면만 진하게.
---
## 14. 접근성 & 글로벌 `[확정/일부 검증대기]`
- 등급·상태를 색만으로 구분 금지 → 색+별 개수/아이콘 병행(색맹). ⚠️ 골든 위 흰/크림 글자 금지(잉크). 골든↔크림 인접은 잉크 외곽선으로 분리.
- 텍스트 최소 `FS_CAPTION`18. 터치 ≥44pt+간격8. 모션 과함 자제(아이리스 0.35s).
- 번역 길어짐 대비: 버튼 고정폭 금지·최소폭 보장, 숫자·아이콘 위주.
- 안드/태블릿 [검증대기].
---
## 13. 미정 / 다음 단계 `[열어둠]`
- [x] §9 토큰 `Design` 오토로드 + 헬퍼(베벨·물건화) — *적용됨(`scripts/style.gd`)*
- [ ] 잘난체/Luckiest Guy 폰트 파일 도입 + 글자효과 실기기 확인
- [ ] 빈티지 질감 오버레이 텍스처(Higgsfield 에셋)
- [ ] 9-slice 프레임·아이콘 텍스처(임시 코드 도형 → 진짜 에셋 교체)
- [ ] 컬러 hex 실기기 검증(크림↔골든 명도)
- [ ] 화면별 적용: 시작 → 홈 → 상점 → 전투준비 → 전투 HUD 순
- [ ] 레퍼런스 보드 `assets/ref/` 저장
