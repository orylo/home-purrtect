# Home Purrtect — UI 디자인 시스템 (design.md)

> **이 문서의 역할**: 바이브 코딩 시 UI가 매번 달라지고 평범해지는 걸 막는 **디자인 규칙(토큰)**. Code는 UI를 만들거나 손볼 때마다 *먼저 이 문서를 읽고* 아래 토큰·규칙을 따른다. "예쁘게"가 아니라 "이 토큰으로".
> **톤 한 줄**: **고전 카툰(Popeye·Cuphead·Tom&Jerry·Felix의 1930s~50s 인쇄물)**. 두꺼운 잉크 외곽선 + 좁고 강한 팔레트 + 빈티지 인쇄 질감 + 굵은 칸 분할.
> **레퍼런스 보드**(작성자 선정): Tom&Jerry 포스터 / Bugs Bunny·Cuphead Funnies 표지 / Popeye 트리뷰트 포스터 / Felix·Peg-Leg Pete / Zizone·Pingo 레트로팝. → 공통 DNA를 아래에 토큰화.
> **기존 자산**: STATUS의 "Caldera 디자인 시스템"(Digital Orange CTA·둥근 알약 버튼)을 *대체가 아니라 정식 확장*. (폰트는 DoHyeon 전량 폐기 → Pretendard[본문]+잘난체/Luckiest Guy[타이틀]로 교체, §2.)
> **태그**: `[확정]` 그대로 / `[열어둠]` 취향 결정 필요.

---

## 0. 핵심 원칙 6 (이걸 어기면 톤이 깨짐) `[확정]`

> 레퍼런스 11장에서 *예외 없이* 공통으로 나온 것 = 이 게임 UI의 헌법.

1. **모든 요소에 두꺼운 잉크 외곽선 + 단순 면.** 버튼·패널·아이콘·HP바·캐릭터 전부 **검정 두꺼운 테두리**. 그라데이션·사실적 음영·반투명 글래스 전부 금지(전 레퍼런스에 0). 면은 색으로 딱딱 나눔. → "구글 머티리얼/부트스트랩 느낌" 나오면 외곽선부터 의심.
2. **좁고 강한 팔레트.** 한 화면에 4~6색. 바탕=크림, 구조=검정 잉크, 주인공/강조=골든·빨강. 그 외 색은 *의미 있을 때만*(파랑=물·밤, 초록=해금 등). Popeye 포스터가 빨강+크림+검정 3색으로 제일 강렬한 걸 기억.
3. **빈티지 인쇄 질감 한 겹.** *이게 "고급 카툰"과 "값싼 클립아트"를 가르는 핵심.* 깨끗한 벡터면 디지털 티 → 살짝 바랜 종이결 + 미세 망점(하프톤) + 1~2px 인쇄 어긋남 느낌을 전체에 깔기. (코드: 오버레이 텍스처 1장 + 살짝 낮춘 채도.)
4. **굵은 칸(그리드) 분할.** 정보가 많은 화면(상점·도감·결과·도전과제)은 Tom&Jerry/Zizone처럼 **굵은 테두리 칸으로 분할**. 칸마다 한 가지 정보. 떠다니는 카드보다 *짜인 칸*이 이 톤.
5. **대칭 + 만화기호.** 패널·타이틀 좌우 대칭, 빈 곳엔 별✦·하트♥·집중선·뼈다귀·💥를 *장식*으로(과하지 않게). 1930s 특유의 "여백을 기호로 채움".
6. **여백은 8의 배수.** 모든 간격·크기를 8px 그리드(8/16/24/32…). 들쭉날쭉 간격 = 아마추어 느낌 1순위 원인.

---

## 1. 컬러 토큰 ★치즈 = 브랜드색 `[확정 / hex는 시작값, 실기기 튜닝]`

> **결정: 메인 = 치즈 골든, 포인트 = 빨강.** 주인공 이름이 "치즈"라 *캐릭터 색이 곧 브랜드 색.* 골든+빨강+크림+검정 = Popeye·Cuphead·Tom&Jerry가 다 쓰는 검증된 고전 카툰 공식(둘 다 따뜻한 계열이라 안 부딪힘, 검정 잉크가 잡아줌).
> **노선: 클래식 1930s 기반 + 채도만 한 톤 쨍하게**(완전 바랜 빈티지로 가면 모바일 작은 화면서 칙칙). 질감은 빈티지, 색은 선명 — Zizone의 쨍함 ↔ Cuphead의 따뜻함 사이.
> Code: 이 값들을 **테마 리소스(`caldera_theme.tres`)·색상 상수 한 곳**에 정의하고, 어디서도 색을 직접 입력하지 말 것(토큰 참조만). 일관성 강제의 핵심.

### 바탕 / 구조 (화면의 90%)
| 토큰 | hex | 용도 |
|---|---|---|
| `ink` (잉크 검정) | `#241F1B` | 모든 외곽선·본문 텍스트. 순흑 아님 — 따뜻한 먹색 |
| `paper` (크림 바탕) | `#F3E3BE` | 패널·카드 기본 배경(바랜 종이) |
| `paper-deep` (진한 크림) | `#E4CB95` | 카드 안쪽 음영·비활성·칸 구분 |
| `canvas` (최하단 배경) | `#2A211C` / 장면 배경 아트 | 화면 바탕(어두운 우드톤) |

### 브랜드 / 강조 (귀하게)
| 토큰 | hex | 용도 |
|---|---|---|
| `cheese` (치즈 골든) ★메인 | `#F2B33D` | **치즈 본인·로고·주요 강조·동전·별.** 바탕으론 안 씀(크림과 뭉갬), 포인트로만 |
| `cheese-deep` (머스타드) | `#D4912A` | 골든 음영·테두리·눌림 |
| `red` (카툰 레드) ★포인트 | `#D6402E` | **CTA(출격·확인·구매)·HP바·위험·강조 타이틀.** 화면당 1~2개 |
| `red-deep` | `#A82B1E` | 빨강 버튼 눌림/그림자 |
| `ink-cream` (역상 글자) | `#F3E3BE` | 잉크 띠·빨강 버튼 위의 글자 |

> **CTA = 빨강, 골든 = 강조/브랜드** 로 역할 분리(둘 다 강조색이라 안 나누면 싸움). 기존 Caldera "Digital Orange"는 → **빨강 CTA로 교체**(레퍼런스 톤이 주황보다 빨강).

### 보조 (의미 있을 때만)
| 토큰 | hex | 용도 |
|---|---|---|
| `teal` (청록) | `#2E8B8B` | 밤·물·차분한 보조(아르데코 제이드) |
| `blue` (코발트) | `#3E6FB0` | 정보·세이브·하늘(Felix/Tom&Jerry 블루칸) |
| `green` (해금) | `#5E8C4E` | 해금·완료 체크 |
| `pink` (재미 포인트) | `#E8729C` | 펄·하트·로맨스 연출 한정(Zizone 핑크의 절제판) |

### 등급 라벨 색 (기존 §0 "이름없는~전설의" 연동) `[확정]`
| 등급 | 토큰 | hex |
|---|---|---|
| Lv1 이름 없는 | `rank-1` | `#9A8C7A` 바랜 회갈 |
| Lv2 떠오르는 | `rank-2` | `#5E8C4E` 그린 |
| Lv3 소문난 | `rank-3` | `#3E6FB0` 사파이어 |
| Lv4 이름난 | `rank-4` | `#8A5BA6` 아미시스트 |
| Lv5 전설의 | `rank-5` | `#F2B33D` 골든(=cheese, 최고 등급=주인공색) |

> 보석 5종: 조약돌=rank-1 / 자수정=rank-4 / 사파이어=rank-3 / 루비=red / 다이아=흰+blue 광택.

---

## 2. 타이포그래피 `[확정]`

### 2-폰트 전략 (한 폰트로 다 하려다 망함 — 역할 분리)
> 레퍼런스 11장도 전부 *타이틀 폰트 ≠ 본문 폰트*다(Zizone·Popeye: 제목은 통통 디스플레이, 작은 글씨는 평범 산세리프). 한 폰트로 다 풀려니 어정쩡해지는 것. **타이틀만 디스플레이, 나머지는 깔끔 산세리프.**

- **본문·UI·숫자 = Pretendard** `[확정·적용됨]` — HP·동전·스킬설명·NPC대사·버튼·가격 전부. 깔끔하고 글로벌(한·영·일·중 커버). *기존 DoHyeon 전량 교체 완료.* **본문은 절대 디스플레이 폰트 쓰지 말 것**(가독성·번역 깨짐).
- **한글 타이틀 = 잘난체(Jalnan2)** `[확정]` — 제목·보스명·"클리어"·로고 등 *큰 글자 한정.* 통통한 카툰 디스플레이 + 상업 사용 무료(라이선스 안전). 쿠키런 대용으로 톤 가장 근접.
- **영문 타이틀 = Luckiest Guy** `[확정]` — 구글폰트(상업 무료). 톰&제리/Popeye식 *굵고 통통한 대문자 카툰체.* "HOME PURRTECT"·"CLEAR!"·영문 로고에. (Bungee·Chewy도 후보였으나 Luckiest Guy가 레퍼런스 통통 대문자에 제일 가깝고 글자 균형이 안정적.)
- ⚠️ **로고/메인 타이틀은 "폰트"가 아니라 "그래픽"으로 끝낸다** — 레퍼런스의 멋짐은 폰트보다 *글자에 입힌 효과*(아래 2.2). 잘난체/Luckiest Guy에 외곽선+섀도우+휨을 입혀야 비로소 카툰 로고가 됨.

### 2.1 타입 스케일 (1560 × 720 UI 좌표 기준 — px) ★실제 좌표
> **중요(좌표계 2개):** UI는 **canvas_items + expand = 1560×720** 좌표로 그려짐 → **타이포는 무조건 720 높이 기준 px.** (배경·바닥선은 1920×1080이지만 글자는 거기 아님.) 아래는 그 좌표의 실제 px.

| 토큰 | px | 폰트 | 용도 |
|---|---|---|---|
| `fs-display-l` | **64** | 잘난체 / Luckiest Guy | 시작 타이틀·보스 이름·게임오버 (화면당 1개, 아치/기울기 OK) |
| `fs-display-s` | **48** | 잘난체 / Luckiest Guy | "클리어!"·막 전환·큰 보상 숫자 |
| `fs-title` | **32** | 잘난체(짧은 제목) / Pretendard 800 | 패널 제목·버튼 글자·NPC 이름 |
| `fs-body` | **22** | Pretendard 500 | 본문·스킬 설명·NPC 대사·가격 |
| `fs-caption` | **18** | Pretendard 700 | 등급 라벨·재료 개수·보조표기 (**720 실질 최저선**) |
| `fs-num` | **28~32** | Pretendard 800 | HP·동전·웨이브 숫자 (외곽선 필수) |

- **위계는 이 6개만.** 중간값(40·26 등) 새로 만들지 말 것 — 위계 흐려지는 1순위.
- **720 높이 최저 = 18px.** 그 아래로 안 감. 작게 더 욱여넣고 싶으면 *글자가 아니라 정보를 줄여라.*
- **버튼 높이 ≥ 60px**(720 좌표, 터치 최소선), 안에 `fs-title`(32).
- 줄간격: body 1.4~1.5 / caption 1.3 / display 1.1.
- **위계 = 크기 + 굵기 + 색 + 효과(2.2)** 네 축으로. 크기만으론 카툰 위계 약함.

### 2.2 글자 효과 — 위계가 높을수록 두껍게 입힌다 ★핵심 (레퍼런스 멋짐의 정체)
> 11장의 "와 멋지다"는 거의 다 *글자에 입힌 외곽선·섀도우·휨*에서 나옴. 위계별로 **효과량을 정확히 차등**한다. 낮은 위계에 디스플레이 효과를 다 주면 화면이 시끄럽고, 높은 위계에 효과가 없으면 밋밋.

| 위계 | 외곽선(stroke) | 그림자(shadow) | 추가 처리 | 비고 |
|---|---|---|---|---|
| **fs-display** (로고·타이틀·보스명) | 잉크 **6~8px**(겹 외곽선 OK: 잉크 굵게+골든/크림 얇게 한 겹 더) | **단색 잉크 드롭섀도우**(블러0, offset 6~8px, 한 방향) | *아치로 휘거나 3~6° 기울임* + 미세 bob 모션 | "로고"는 여기 + 색 채움(빨강/골든)까지 = 그래픽 취급 |
| **fs-display-s** ("클리어"·보상) | 잉크 4~5px | 단색 섀도우 offset 4~5px | 톡 튀는 등장(scale bounce) | |
| **fs-title** (패널제목·버튼·NPC명) | 잉크 **2~3px** | 옅은 단색 섀도우 offset 2px *또는 생략* | — | 버튼 위 글자는 외곽선만, 섀도우는 버튼이 가짐 |
| **fs-body** (본문·대사) | **0~1px**(크림 위면 0, 배경아트 위면 1px) | 없음 | — | 본문에 두꺼운 외곽선 = 가독성 사망. 최소만 |
| **fs-caption** (라벨·캡션) | 0 (색 대비로만) | 없음 | — | 효과 없음. 등급색 등 *색*으로 구분 |
| **fs-num** (HP·동전·데미지) | 잉크 2~3px | offset 2px | 변할 때 scale 튀고 카운트업(§5) | 동전=골든 채움, 데미지팝업=display 취급 |

**규칙 요약 (외우기 쉽게):**
- **외곽선 두께 ∝ 글자 크기.** display 6~8px → title 2~3px → body 0~1px → caption 0. *작은 글자에 두꺼운 외곽선 금지*(뭉개짐).
- **그림자(blur 0, 단색, 한 방향).** 카툰 입체는 *블러 그림자*가 아니라 *딱딱한 단색 오프셋.* display만 크게(6~8px), 나머지는 약하게/없음.
- **휨·기울임은 fs-display 한정.** 본문·라벨은 똑바로(가독성).
- **색 채움 + 효과 = "로고/타이틀".** 평범한 글자에 빨강/골든 채우고 외곽선 두껍게 입히면 그게 로고. Code에 "이 텍스트는 fs-display 효과로"라고 지시하면 됨.
- **숫자는 항상 외곽선 2~3px.**(작아도 예외 — 게임 숫자는 튀어야) 동전 숫자 골든, HP 숫자 크림.

> Code 지시 예: *"제목은 fs-display(잘난체, 잉크 외곽선 7px + 골든 얇은 겹선 + 단색 잉크 섀도우 offset 7px, 4° 기울임). 본문은 fs-body(Pretendard 500, 외곽선 0). design.md §2.2 표 그대로."*

---

## 3. 컴포넌트 규칙 `[확정]`

### 버튼
- **모양**: 둥근 알약 + **검정 외곽선 3~4px** + 아래쪽 **단색 잉크 그림자**(블러 X, offset 4~6px) = 카툰 입체.
- **위계 = 색으로만, 3종:**
  - **주요(CTA)**: `red` 채움 + 검정 외곽선 + 크림 글자. 화면당 1개(출격·확인·구매).
  - **보조**: `paper` 크림 채움 + 검정 외곽선 + 잉크 글자.
  - **브랜드/특별**: `cheese` 골든 채움(레벨업·펄·보상 등 "좋은 것"). 골든은 *버튼 채움까진 OK*, 단 큰 패널 바탕엔 금지.
- **눌림**: 그림자 사라지고 버튼이 그림자 자리로 쑥(클래식 카툰 누름). press 꼭 시각 변화.
- **금지**: 그라데이션 채움, 블러 그림자, 글래스모피즘.

### 패널 / 카드
- 크림(`paper`) 바탕 + 검정 외곽선 4~6px + 모서리/상단에 작은 기하 장식. 둥근 모서리 약하게(8~12px).
- **상단 타이틀 띠**: 패널 맨 위 잉크 띠 + 크림 역상 글자(레퍼런스의 "제목 검정 박스" 패턴). 또는 빨강 띠.
- 내부 구분선 = 얇은 더블라인(잉크 또는 골든).

### 칸 그리드 (정보 많은 화면) ★레퍼런스 핵심
- 상점·도감·결과·도전과제 = **굵은 잉크 테두리 칸**으로 분할(Tom&Jerry/Zizone). 칸마다 한 정보(아이템 1개·캐릭터 1명). 칸 사이 간격 8px, 테두리 3~4px.
- 칸 안: 위=그림(크림 바탕), 아래=이름 띠(잉크 또는 색 띠 + 역상 글자). 레퍼런스의 "그림+캡션 띠" 그대로.

### HP바 (전투 상단)
- `red` 채움 + 검정 외곽선 + **숫자 병기**(직업·Lv별 130~650). 좌측 ❤️/고양이머리 아이콘.
- 줄 때 흰 잔상 잠깐 남았다 따라옴(카툰 타격감).

### 아이콘
- **단색 실루엣 + 동일 굵기 검정 외곽선**(다색 일러스트 금지). 동전=골든, 보석=jewel색, 스킬/아이템=크림 원형 뱃지 안 잉크 실루엣.
- 만화기호(별·하트·💥)는 아이콘과 같은 잉크 굵기로.

---

## 4. 레이아웃 / 안전영역 (가로 모드 iOS) `[확정]`

- **실전 전략 = "16:9 세이프존 + 19.5:9 풀블리드".** 핵심 UI(버튼·게이지·정보)는 가운데 **16:9 안에 전부**, 배경 등 비핵심만 좌우로 19.5:9까지 늘려 채움 → *어떤 기기에서도 중요한 게 안 잘림.*
- **디자인 캔버스**: **852 × 393 pt**(iPhone 15/14, 19.5:9)로 그림. 아이패드(4:3)까지 보려면 **1024 × 768**에서도 세이프존 확인.
- **안전영역(env safe-area-inset)**: 가로 모드에선 노치·홈 인디케이터가 **좌/우 단변**에 옴 → **양옆 ~44pt 패딩**을 꼭 비움(`env(safe-area-inset-left/right)`). 하단 홈 인디케이터 영역도 비움.
- **HUD(기획 §7-B 토큰화):**
  - **상단 얇은 띠**: 좌=HP바+숫자 / 중=웨이브 N/M / 우=일시정지 ≡. 반투명 잉크 띠 + 크림 글자.
  - **하단 조작 행**: 좌=이동/점프 / 중=소모품 2~3 / 우=스킬 2~4 + 동료 + 근접·원거리. **자리는 최대치 확보**(늘어도 안 깨지게).
  - 위 HUD 전부 **16:9 세이프존 + 44pt 인셋 안쪽**에. 배경 아트만 풀블리드.
- **8px 그리드 / 터치 타겟 최소 44×44pt**(애플 HIG 최소; 가로 엄지엔 더 크게 권장).

---

## 5. 연출 / 모션 (저비용 고임팩트) `[확정]`

카툰 톤은 *움직임*에서 절반. 코드로 싼 것:
- **버튼 등장**: 통 튕김(scale 0.8→1.05→1.0 bounce).
- **패널 열림**: 탁 슬라이드 + 살짝 오버슈트.
- **숫자 변화**(동전·HP·데미지): 카운트업 + 톡 튀는 scale.
- **화면 전환**: **검정 아이리스(원형 조리개) 닫고 열기** — 1930s 시그니처. 막 전환·게임오버에 특히.
- **everything bobs**: idle 요소(타이틀·치즈·버튼)가 미세하게 위아래 들썩.
- 금지: 밋밋한 페이드/슬라이드만 — 정답은 *과장된 탄성*.

---

## 6. 사용법 — Code에게 거는 법 `[확정]`

> "`design.md`를 먼저 읽고, 그 토큰(색·타이포·글자효과·간격·버튼·외곽선 규칙)을 지켜서 [화면명]을 만들어/고쳐줘. 새 색·새 폰트 만들지 말고 토큰만 참조. 본문=Pretendard, 타이틀만 잘난체/Luckiest Guy. 8px 그리드·검정 외곽선·CTA 빨강 1개·치즈 골든은 강조로만·글자효과는 §2.2 위계표대로."

- **레퍼런스와 함께**: "이 스샷의 칸 분할/여백 느낌으로, 색은 design.md 토큰으로."
- **고칠 때**: "이 화면이 design.md 어떤 규칙을 어기는지 먼저 짚고 그것만 고쳐줘"(막 갈아엎기 방지).
- **토큰을 코드 한 곳에 박아라**: 색·간격·폰트크기를 theme 리소스/상수 파일 하나에 → 한 번 바꾸면 전체 일관. "수준 낮아 보임"을 구조적으로 막는 핵심.

---

## 7. 빠른 체크리스트 (화면 하나 끝낼 때마다) `[확정]`

- [ ] 본문은 Pretendard, 타이틀만 잘난체/Luckiest Guy인가?
- [ ] 글자 외곽선이 위계대로인가(display 두껍게~body 0~1px, §2.2)?
- [ ] 그림자가 단색·블러0·한방향인가(블러 그림자 아님)?
- [ ] 모든 버튼·패널·아이콘에 검정 외곽선 있나?
- [ ] CTA(빨강) 버튼이 화면에 1~2개뿐인가?
- [ ] 골든을 *치즈·강조·동전*에만 썼나(큰 바탕에 안 깔았나)?
- [ ] 색이 토큰 안에서만 쓰였나?
- [ ] 빈티지 질감 한 겹이 깔렸나(깨끗한 벡터 티 안 나나)?
- [ ] 간격이 전부 8의 배수인가?
- [ ] 터치 버튼 44pt 이상 / 16:9 세이프존 + 44pt 인셋 안인가?
- [ ] 등장·눌림에 탄성 모션이 있나?

---

## 8. 미정 / 다음 단계 `[열어둠]`

- [ ] 잘난체/Luckiest Guy 웹폰트 로딩 검증(웹빌드 한글·라이선스 동봉 방식) + 720 좌표서 글자효과(외곽선 px) 실기기 확인
- [ ] 빈티지 질감 강도(망점·바램) 실기기 검증 — 약하면 디지털 티, 세면 지저분
- [ ] 컬러 hex 실기기 검증(크림이 OLED서 너무 노랗지/골든과 안 뭉치는지)
- [ ] 칸 그리드 공용 컴포넌트 1세트(그림+캡션 띠) — 상점·도감 재활용
- [ ] 화면별 적용 우선순위(시작→홈→전투 HUD→상점 추천)
- [ ] 레퍼런스 보드 11장 `assets/ref/`에 저장(Code·아트 공용)
