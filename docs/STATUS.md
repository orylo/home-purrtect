# Home Purrtect — 구현 현황 & 로드맵 (STATUS)

> **이 문서의 역할**: 기획서(`기획_*.md` = "무엇을 왜")와 **실제 코드(현실)** 사이의 다리.
> 기획 챗은 이 문서를 보고 "지금 실제로 뭐가 돌아가는지" 파악한 뒤 신 기획을 이어가면 됩니다.
> **갱신 주체**: 개발(Claude Code)이 커밋할 때마다 같이 업데이트. **기획서 원본은 챗이 주인.**

- **현재 버전**: `0.0.141`  (버전 체계 `X.Y.Z`: X=출시·대폭변경 / Y=막 완성·큰 이벤트(`0.1.0`=1막 완전 완성) / Z=자잘한 업데이트마다 +1, 99→100 자리올림 없음. `1.0.0`=3막 완성 첫 출시. CLAUDE.md "버전 체계" 참조)
- **라이브(웹)**: https://orylo.github.io/home-purrtect/
- **엔진**: Godot 4.6.3 / 출시 타깃: iOS 먼저 → 안드로이드
- **마지막 갱신**: 2026-06-05
- **✅ 시스템 알림 ↔ 내레이션 통합(2026-06-05, v0.0.141)**: 대화창 비-화자 텍스트를 `sys` 하나로 통합(별도 `narr` 무음 플래그 제거). 대화창 페이지 = **화자(이름·초상화·목소리) / sys(가운데·무이름·시스템음)** 2종. 코드상 대화창 내레이션은 게임오버 recap "치즈가 싹싹 빈 덕분에…" 한 줄뿐이라 그 줄을 `sys`로 전환(나머지 내레이션=프롤로그 자막은 대화창이 아닌 상단 자막 별도 처리). 검증: main 무에러.
- **✅ 펄 돌 건넴 = 시스템 알림(2026-06-05, v0.0.140)**: 펄 컷씬의 "빛나는 조약돌을 펄에게 건넸다"는 지문이 아니라 **아이템 헌납(치즈 아이템 −1) 시스템 이벤트** → `sys`(시스템 알림음·가운데·무이름)로 변경. (v139에서 `narr`로 잘못 분류했던 것 정정. `narr` 무음 플래그 메커니즘은 향후 진짜 지문용으로 유지.) *실제 인벤토리 차감은 플레이스홀더 컷씬이라 미연결 — 연출 문구만.* 검증: main 무에러.
- **✅ 클리어 후 컷씬 BGM = 비전투(2026-06-05, v0.0.138)**: 전투 클리어 후 인게임 이벤트→컷씬(1-3/5/7/13/16)이 main.tscn 위 오버레이라 **전투 BGM이 계속 흐르던 것** 수정. `_on_inscene_event`에서 컷씬 시작 시 `Music.set_override("menu")` → 전투 BGM 종료, 비전투 트랙(테스트로 `menu`=Going Bananas) 재생, 컷씬 끝→홈도 menu라 끊김 없음. *추후 컷씬 전용곡 생기면 교체.* (구조 메모: 컷씬을 별 씬 대신 오버레이로 두는 건 **줌인 연출이 전투 화면을 스냅샷해 확대하기 때문** — 의도된 설계. 프롤로그는 독립 씬.) 검증: game 구문·main 무에러.
- **✅ 디버그 오버레이 가로배치·반투명·터치차단(2026-06-05, v0.0.137)**: 직전(v136) 디버그 패널 후속 3건 — ① **반투명 패널**(어두운 BG 알파 0.42) → 하단 게임영역(치즈·조작)이 비쳐 보임. ② **가로로 넓고 낮은 4칼럼 배치**(치트 / 전투·스킬 / 스테이지·직업 / 스폰5열) — 상단 띠(y58~)에만 자리, 세로로 안 길어 치즈를 덜 가림. ③ **터치 차단** — 디버그 버튼/패널 위 터치가 조이스틱 점프로 새던 것 수정: `Touch.register_block()`/`is_blocked(pos)` 신설(보이는 차단 Control 등록), `joystick._handle_press`가 차단 영역이면 무시. 검증: 3스크립트 구문·main 무에러.
- **✅ 디버그 오버레이 가독성 + 이벤트/웨이브 스킵(2026-06-05, v0.0.136)**: ① **가독성** — 🐞(버그) 패널이 게임 테마(밝은 버튼)에 진한 글씨가 묻혀 안 보이던 것 → 디버그 전용 스타일(어두운 불투명 패널 BG_PANEL + **크림 버튼 PAPER + 잉크 글씨 INK**(전 상태) + 골드 헤더 + 2px 테두리), SpinBox 내부 LineEdit도 크림+잉크. ② **이벤트 스킵(전투만) 토글** — `cheats["skip_events"]`: ON이면 stage_intro 미생성(펑거스 인트로/아웃트로/팝업 생략)·클리어 후 컷씬(1-3/5/7/13/16)·게임오버 컷씬 전부 건너뜀 = 순수 전투. (인트로는 _ready에서 판정 → 다음 스테이지 진입부터 적용, 컷씬류는 즉시.) 버튼이 ON/OFF·색(빨강=ON) 표시. ③ **웨이브 건너뛰기 버튼** — `spawner.dev_skip_wave()`: 남은 스폰 큐 비우고 생존 적 전멸 → 다음 웨이브(또는 클리어). 검증: 4스크립트 구문·main 헤드리스 무에러.
- **✅ 쾌청 날씨 태양(2026-06-05, v0.0.135)**: 맑음(`calm`) 날씨일 때 **태양**을 화면에 추가 — **중심이 화면 최상단(y=0)**에 걸려 **하단 반원만** 보임, 가로 중앙. `halftone_bg.gd`가 날씨를 정하므로 거기서 `_weather=="calm"`일 때만 `Sun` 노드 생성(항상 동기화). 빈티지 카툰 톤(골든 F2B33D + 밝은 속 FBD774 + 잉크 외곽 + 옅은 후광 3겹), 반지름=화면높이×0.22. Halftone 자식이라 하늘에 떠 지면·플레이어 뒤. calm 고정 스테이지=1-1·1-2(+랜덤풀 calm). 검증: main(1-1=calm) 헤드리스 무에러·PIL 배치 미리보기.
- **✅ 골드 보이스 미세조정(2026-06-05, v0.0.134)**: 과한 저음 완화 `sample_pitch` 0.62→0.70(살짝 덜 낮게) + 말 속도 `char_ms` 42→48(좀 더 느릿).
- **✅ 골드 영감 보이스(맥스 소스 저음 변주)(2026-06-05, v0.0.133)**: 골드 목소리 = **맥스 녹음 소스 기반**이되 ① **더 긴 음절(200ms, 맥스 120ms보다 원본이 더 들림)**·살짝 크게 → `assets/audio/voice/blip_gold.wav`, ② **더 낮게** 재생. 샘플은 `pitch_scale≈freq/base≈1`이라 base_freq만으론 안 내려가서 **`sample_pitch` 프로필 필드 신설**(샘플 재생 피치 배율, 기본 1.0) → `blip()`이 `(freq/base)*sample_pitch`로 변조. `gold` 프로필(triangle·blip_every2·char_ms42·**sample_pitch 0.62**)=느릿·저음 노인. **연결**: 프롤로그 골드 영감 대사 2개를 즉시표시→**타이핑+골드 보이스**(세대카운터로 장면전환 경합 방지, 탭=즉시완성) + 게임오버 전보 "골드 :" 페이지에 `"voice":"gold"`(컷씬 페이지별 voice 지정 지원 추가). 검증: gold import·prologue/main 헤드리스 무에러.
- **✅ 시스템 알림음 = 합성음 분리(2026-06-05, v0.0.132)**: 펑거스에 **녹음 음절**을 연결한 뒤 "fungus" 프로필을 쓰던 시스템 알림까지 녹음 펑거스 목소리로 바뀌어버린 것 수정. **별도 `system` 프로필 신설**(합성음=`sample_path` 빈칸, square·220Hz·32ms = 녹음 펑거스와 구분되는 "기계 비프"). `VoiceBlip.notify()`·컷씬 sys 페이지(`_play_cutscene`)를 `"fungus"`→`"system"`으로 전환. 의도: 시스템 읽어주는 소리 = **처음 절차합성한 그 소리**(NPC 녹음과 다른 톤). 검증: main 헤드리스 무에러.
- **✅ 펑거스 보이스 속도 완화(2026-06-05, v0.0.131)**: 녹음 음절(120ms) 적용 후 펑거스가 char_ms=22ms로 너무 빨라 블립이 겹쳐 뭉치던 것 → **34ms**(펄45 ↔ 기존22의 중간)로 조정해 음절 간 겹침↓. 시스템 알림도 펑거스 프로필이라 함께 완화. (펄·맥스 미변경.)
- **✅ 컷씬 시스템 알림 분리(2026-06-05, v0.0.130)**: 펄 첫만남 컷씬 등에서 **NPC 대사 + 시스템 알림(호감도·해금)이 한 대화창에 섞여 있던 것**을 별도 페이지로 분리. 컷씬 페이지에 `"sys": true` 플래그 도입 → `_play_cutscene`이 시스템 페이지는 **펑거스 음색**으로, `_cut_page(…, sys)`가 **초상화·이름표 없이 박스 전체폭 가운데 정렬**로 렌더(화자 대사와 시각적 분리). 적용: 펄(호감도↑·축복 해금)·맥스(상점·제작 해금)·비둘기(동료 시스템 해금)·치와와(합류)·보안관(획득) — 모든 해금/획득 알림이 화자 대사 다음 별도 가운데정렬 페이지로. 검증: game 구문(autoload만 미해결)·main 헤드리스 무에러. *실제 음색·정렬은 F5/웹.*
- **✅ NPC 녹음 음절 보이스 + 프롤로그 대화창 통일(2026-06-05, v0.0.129)**: ① **녹음 음절 적용** — `Desktop/Claude/voice`의 펑거스·펄·맥스 m4a를 macOS 내장 `afconvert`로 디코드(ffmpeg 미설치) 후 Python으로 **가장 또렷한 한 음절(슬라이딩 RMS 최대 ~120ms 구간) 추출+양끝 5ms 페이드+노멀라이즈** → `assets/audio/voice/blip_{fungus,pearl,max}.wav`(22.05kHz 모노, 무손실). `voice_blip.gd` `VOICE_PROFILES`의 `sample_path` 연결 → 합성음 대신 녹음 음절을 `pitch_scale`(≈1±jitter) 변조 재생(스왑 훅 기존 구현). *스펙은 .ogg였으나 ffmpeg 부재로 .wav(웹 export 시 자동 압축, 기능 동일). 트림 길이 120ms는 튜닝 가능 — 빠른 화자(펑거스 22ms)에서 겹치면 더 짧게.* ② **시스템 알림=펑거스 음색** — `VoiceBlip.notify()`(즉시표시 알림용 펑거스 단발 블립) 추가, 해금 이벤트 카드(`event.gd`)가 표시 시 1회 호출(별도 system 프로필 없이 fungus 그대로). 타이핑 시스템 알림은 `blip("fungus",…)` 사용. ③ **프롤로그 대화창 통일** — 프롤로그 골드 영감 대화창(자체 Panel)을 **공용 `DialoguePanel`로 교체**(초상화 에셋 없어 portrait 생략·본문 왼쪽부터, 진행 힌트는 기존 우하단 _hint로 통일). 자막(cap, 상단 내레이션)은 대화창 아니라 유지. 검증: voice import 정상·prologue/main 헤드리스 무에러. *실제 소리·정렬은 F5/웹.*
- **✅ 대화창 프레임 나인패치(2026-06-05, v0.0.128)**: `ui_dialogue.png` 프레임을 `DialoguePanel`에서 **NinePatchRect**로(마진 L/R=150·T/B=100px). 코너 플로리시는 고정 크기, 테두리선·가운데 양피지만 늘어남 → **어떤 화면비(16:9·21:9·4:3)에서도 코너 왜곡 0**, 화면 좁아지면 가운데만 짧아짐. (종전 STRETCH_SCALE은 가로 ~7% 압축됨.) 검증: main 무에러·PIL 나인패치 시뮬(16:9/21:9 코너 동일).
- **✅ 대화창 커스텀 프레임 테스트(2026-06-05, v0.0.127)**: 사용자 제작 UI 에셋 `ui_dialogue.png`(빈티지 양피지 테두리, 2255×322·7:1, UI라 무손실)를 `assets/ui/`로 복사 후 **DialoguePanel 배경으로 적용**(절차적 StyleBox → 박스=Control + TextureRect 프레임을 박스 크기로 늘림). 초상화칸·이름·본문은 그 위. 전 대화창(펑거스·펄·맥스 컷씬) 공통 반영. *튜닝 여지: 우하단 탭힌트가 코너 플로리시와 살짝 겹침·초상화 칸 스타일·정확 배치(에셋 _full 가이드는 하단 89%폭/바닥45px).* 검증: main 무에러·PIL 미리보기.
- **✅ ground06 교체 + 1-3 고정 확인(2026-06-05, v0.0.126)**: 기존 ground06(담장+잔디) 삭제 후 신규 이미지(생성형 채우기 16 — 담장+나무상자·통, 흙바닥, 2520×1080·투명13%)로 대체(손실압축). 1-3은 이미 far02/ground06 고정이라 코드 변경 없이 새 그림 적용(고정 지면이라 랜덤 풀 제외도 유지). PIL 합성 검증: 발선이 흙바닥에 정렬.
- **✅ 대화창 통일 — 공용 DialoguePanel(2026-06-05, v0.0.125)**: 게임 내 모든 NPC 대화창을 "인게임 이벤트 펑거스 대화창" 스타일로 통일. **`scripts/dialogue_panel.gd`(class_name DialoguePanel)** 신규 — 크림 패널(PAPER)+잉크 외곽+초상화칸+골드 이름(CHEESE_DEEP)+잉크 본문+탭 힌트를 한 곳에서 빌드(`build(parent,vp,portrait)` → 노드 dict). `stage_intro._build_ui`(펑거스)·`game._cut_page`(펄/맥스 등 컷씬) 둘 다 이걸 사용 → 동일. 컷씬은 화자(voice)로 이름·초상화색 매핑(`CUT_VOICE_NAME/COL`: 펄=분홍·맥스=갈색 플레이스홀더, 스프라이트 없어 단색), 그림 플레이스홀더·페이지카운터는 유지. 타이핑+재잘 보이스는 그대로. 검증: 3파일 무에러·main 로드.
- **✅ 배경 고정 규칙 명확화(2026-06-05, v0.0.124)**: **원경 고정 복원**(1-1=far08·1-3=far02·1-20=far01) — 단 고정 원경은 **랜덤 풀에서 제외 안 함**(다른 스테이지에서도 랜덤으로 나올 수 있음). **지면 고정**(1-1/1-3/1-10/1-20)은 **랜덤 풀에서 제외**(다른 스테이지 미등장). 즉 far=고정+풀유지 / ground=고정+풀제외. 검증: far 풀 미제외·ground 랜덤 6장·main 무에러.
- **✅ 배경 고정/랜덤 정리(2026-06-05, v0.0.123)**: ① **원경 전부 랜덤**(FIXED_FAR 비움). ② **지면 고정 4건**: 1-1=ground04·1-3=ground06·**1-10=ground09(흙)**·1-20=ground03. ③ **고정 지면은 랜덤 풀에서 제외**(`_pick_seq` exclude에 `FIXED_GROUND.values()` 추가) → 다른 스테이지엔 안 나옴(랜덤 가용 지면 6장: 01·02·05·07·08·10). 검증: 딕셔너리·제외목록·main 무에러.
- **✅ 스테이지별 배경 고정 3건(2026-06-05, v0.0.122)**: `stage_background` FIXED_FAR/FIXED_GROUND에 — **1-1**=far08/ground04 · **1-3**=far02/ground06 · **1-20**=far01/ground03 고정(그 외 스테이지는 풀 랜덤 유지). 검증: 경로 6개 존재·main 무에러.
- **✅ 옛 배경 에셋 삭제(정리)(2026-06-05, v0.0.121)**: 이제 안 쓰는 옛 배경 일괄 삭제 — `wall/far`(옛 16:9 원경 12 + fixed)·`wall/ground`(옛 지면 21 + fixed)·`wall/test`(BG_Far1/Ground1 테스트쌍). 헷갈림 방지. `stage_background` DEFAULT 폴백을 새 풀(far_full/ground_full)로 변경, POOL_COUNT는 near만 유지(far/ground 죽은 카운트 제거)+주석 갱신. **유지**: `wall/near`(랜덤 근경)·`stage1_wall.jpg`(npc_talk·shop 사용). 검증: 잔존 참조 0·main 헤드리스 무에러.
- **✅ 1막 배경 소재 가공·풀 적용(2026-06-05, v0.0.120)**: 기획폴더 `BG/BG real` 18장(2520폭)을 투명비율로 분류 — **원경 8장(꽉참)·지면 10장(투명 있음)**. 세로<1080 지면 2장(752·738)은 **상단 투명 패딩으로 1080화**(바닥 정렬). `assets/backgrounds/wall/far_full/far01~08.png`(RGB)·`ground_full/ground01~10.png`(RGBA)로 손실압축 저장. `stage_background`를 **풀에서 매 판 far·ground 각각 랜덤(직전 판과 다르게)** 으로 변경(`_pick_seq`, FULL_*_COUNT, `_last_far/_last_ground`) — 풀프레임 유지, FIXED override·DEFAULT 폴백 보존, 근경 랜덤 미변경. PIL 합성 검증: 원경이 지면 투명부로 비치고 발선(310)이 보행면에 정렬. *흙(ground09)은 톤이 달라 랜덤 등장 시 튀면 제외/전용배정 가능.* 검증: 18장 로드·main 헤드리스 무에러.
- **✅ NPC 재잘거림 보이스(절차적 합성, 펑거스/펄/맥스)(2026-06-05, v0.0.119)**: 동물의숲/Undertale식 — NPC 대사가 한 글자씩 타이핑될 때 짧은 블립음으로 "지껄이는" 분위기(실제 단어 안 들림, 분위기용). 치즈는 무대사라 미적용. **외부 에셋 0개**: `scripts/voice_blip.gd`(오토로드 `VoiceBlip`)가 AudioStreamWAV를 런타임 합성(파형 sine/square/triangle 1개씩 캐시 + 글자마다 `pitch_scale` 변조), `AudioStreamPlayer` 6풀. **`VOICE_PROFILES`**(한 곳 튜닝): fungus(220Hz·square·jitter0.15·매글자·22ms) / pearl(330·sine·0.05·2글자·45ms) / max(180·triangle·0.10·2글자·35ms). 억양: ?↑ / …·.↓ / 진행도 완만변동. 공백·문장부호 무음. **샘플 .ogg 스왑 훅**: 프로필 `sample_path` 채우면 합성 대신 음절 pitch 변조(현재 전부 빈칸=합성). **타이핑 연출 신규**(기존엔 즉시표시): `stage_intro._type_text`(펑거스, `visible_characters` 한글자씩+blip, 탭=스킵→전체표시→다음) + `game._cut_page`(voice 파라미터, 펄=`_event_pearl`/맥스=`_event_max`에서 전달). 게이팅: `GameState.voice_enabled`(기본 ON·저장, VoiceBlip이 읽음) — *사운드 설정 UI 훅은 추후(현재 홈 [설정]은 BGM 토글뿐)*. 검증: 합성 동작(blip_every 스킵·char_sec·파형 캐시)·main 헤드리스 무에러. *실제 소리는 F5/웹.*
- **✅ 침입자 공격 크리티컬(길냥이와 통일)(2026-06-05, v0.0.118)**: 침입자 평타(근접·탄환)도 크리 가능. **크리율·배율 = 길냥이(base)와 동일**(`GameState.JOB_STATS["base"]` 참조 = 5%·strike ×1.5, 넉백·스턴 없이 데미지만 조금 강하게). `enemy._crit_dmg()`(근접 `_resolve_attack`·원거리 `_fire_projectile`에서 굴림) → `player.take_damage(dmg, crit)`. 탄환은 `enemy_bullet.is_crit` 플래그로 전달. 피격 연출도 크리 차등: 크리=`Sfx.impact(true)`·`critical_hit` 버스트·흔들림7·**주황 큰 숫자(-N!)**, 일반=punch·impact_flash·흔들림3·빨강. 검증: 무에러·critical_hit 에셋 확인.
- **✅ 치즈 피격 타격 연출 추가(2026-06-05, v0.0.117)**: 치즈가 침입자 공격에 맞을 때 색 플래시만 있고 **소리·임팩트 이펙트가 없던** 것 → 적이 치즈 평타에 맞을 때와 **동일**하게 `player.take_damage`에 `Sfx.impact(false)`(punch) + `Fx.request_shake(3.0)` + `Fx.burst("impact_flash")`(몸통 −110) 추가(실데미지 amount≥1일 때만). 검증: player 무에러·impact_flash 에셋 확인.
- **✅ 발사체 4종 정비 + 신규적 그림자 문서화(2026-06-05, v0.0.116)**: ① **투척쥐 돌 색** — `enemy_bullet._draw_stone`이 `color`에서 음영 파생하게 바꿔, 회색 투척쥐=치즈 돌과 동일 톤(0.55), 검은 투척쥐=어둡게(0.26, 털색). ② **거미 발사체 = 흰 거미줄**(`shape:web` 신규 — 방사 스포크+동심 거미줄, 블랙 스트로크 없이 흰색, 천천히 회전). ③ **박쥐 발사체 = 초음파**(`shape:sonic` 신규 — 진행방향 동심 호 3겹+맥동, 밝은 청백). `_fire_projectile`에서 `_id`별 shape·색 지정. ④ **문서화**: `지침_공격스프라이트.md`에 **F(발밑 그림자 정렬 — 발 실측→`SHADOW_FOOT` 등록 필수) / G(원거리 발사체 모양·색)** 섹션 추가 + CLAUDE.md 포인터 → 앞으로 적 추가 시 그림자 자동 정렬. 검증: enemy/enemy_bullet 무에러 + PIL 미리보기.
- **✅ 그림자 살짝 위로(접지감 개선)(2026-06-05, v0.0.115)**: 그림자가 발 접지점보다 아래에 찍혀 "바닥 아래 유리판" 느낌이던 것 → 모든 발밑 그림자(치즈·적·펑거스)를 **자기 가로반경 × `Layout.SHADOW_LIFT_FRAC`(0.18)** 만큼 위로(크기 비례: 치즈 14·쥐 10·거미 20·펑거스 15·벌 7px). 한 곳(Layout 상수)에서 튜닝. 공중 적은 높이 축소(sh_t)에 lift도 비례. 검증: 4파일 무에러.
- **✅ 그림자 정렬 3종 수정(2026-06-05, v0.0.114)**: 실측(스프라이트 알파 발 bbox) 기반. ① **좌측 쏠림** — 발 중심이 프레임 중심보다 오른쪽(치즈 +18·쥐 +36·롤러 +28 프레임px)이라 그림자(중심)가 발 왼쪽에 찍히던 것 → **그림자 x를 발 중심으로 이동**(flip 반영). `enemy.gd` `SHADOW_FOOT` dict(적별 [발중심, 발반폭] 프레임px) + `_sprite_sc`로 환산, `player.gd`는 치즈 +18(항상 우향). ② **너무 작음** — 거미(발폭 306)·투척쥐(322) 등은 그림자 반경 = **`max(_body_r×배율, 발반폭×스케일)`** 로 → 넓은 적만 발폭만큼 커지고 쥐는 유지. ③ **펑거스 그림자 없음** — `stage_intro._draw`에 펑거스 NPC 발밑 그림자(검정 30% 납작 타원, 이동 추종) 추가. 검증: 3파일 구문·main 헤드리스 무에러(실제 정렬 F5/웹). *공중 적은 높이연동 축소 유지.*
- **✅ 바닥선 = 화면높이 비율식(배경 바닥선과 항상 일치)(2026-06-05, v0.0.113)**: PC(16:9)와 폰(19.5~21:9)에서 캐릭터 발선이 배경 바닥선과 어긋나던 문제. 원인 = 배경은 height-fit이라 바닥선이 **화면높이 고정비율**(310/1080≈아래 28.7%)인데 `Layout.ground_y`는 **화면 아래 고정픽셀(206px)**이라, 뷰포트 높이가 같은 폰(stretch expand로 항상 720)에서만 우연히 일치하고 PC(16:9=877)에선 ~46px 어긋남. **수정**: `ground_y() = _vis().y × GROUND_LINE_FRAC`(= 1−310/1080 ≈ 0.713) **비율식**으로 → 폰 207·PC 252·아이패드 336px(아래서) 모두 **배경 바닥선과 정확히 일치**(폰은 종전 206→207, 1px·체감 없음). 조작 띠·버튼(`band_top`/`bottom_row`)은 종전대로 **하단 고정픽셀+safe_bottom** 유지(발선과 분리). `ground_y`는 safe_bottom 안 뺌(배경 풀블리드라 바닥은 배경 비율 따름). `CONTROL_BAND_GAP`/`GROUND_DROP`은 미사용으로 전환(상수만 보존). 검증: 비율값 대조·main 헤드리스 무에러. *실제 PC/폰 정렬 일치는 F5/웹 확인.*
- **✅ 전 스테이지 = 신규 21:9 풀프레임 배경 기본 적용(2026-06-05, v0.0.112)**: 1-2 테스트 OK 확인 후, **모든 스테이지가 BG_Far1/BG_Ground1 풀프레임 배경**을 쓰도록 기본값화(사용자가 스테이지별로 이미지 수동 교체 예정). `stage_background._pick_backgrounds`: `_fullframe=true`·`_far_lift=0` 고정, far/ground = **`FIXED_FAR/FIXED_GROUND.get(key, DEFAULT_FAR/DEFAULT_GROUND)`** → 스테이지별 override 없으면 공통 DEFAULT. **per-stage 교체 = `FIXED_FAR/FIXED_GROUND`에 `"막-스테이지":"경로"` 한 줄 추가**(현재 둘 다 비어 있어 전부 DEFAULT). 옛 16:9 랜덤 풀·고정(1-3/1-10/1-20)·지면 휴리스틱 경로는 미사용(코드는 보존). **근경(랜덤)은 미변경**. 검증: DEFAULT 경로 존재·override 비어있음·main 헤드리스 무에러.
- **✅ 1-2 신규 21:9 풀프레임 배경 테스트(2026-06-05, v0.0.111)**: 기획 폴더 `New_BG_Test`의 **BG_Far1**(원경=저택·정원·하늘)·**BG_Ground1**(지면=돌담·화단·돌길·잔디, 위쪽 투명) 2장(둘 다 2520×1080)을 `assets/backgrounds/wall/test/`로 복사(손실압축 import)하고 **1-2에 적용**. 가이드의 "21:9 그림 나오면 height-fit 전환" 시점 → **풀프레임 경로 신설**: `FIXED_FAR["1-2"]`·`FIXED_GROUND["1-2"]` + `FULLFRAME_STAGES`(`_fullframe`). 풀프레임이면 **far·ground 둘 다 같은 커버 스케일(높이맞춤·바닥고정)로 꽉** 그림(`ground_layer`가 `_fullframe` 읽어 휴리스틱 OFF) + **원경 패럴랙스 올림 OFF**(`_far_lift=0`). 뷰포트 비율 ≤ 21:9라 커버=높이맞춤(h=vis.y) → far·ground 자동 정렬, 설계 바닥선(310px)이 ground_y에 자동 일치. **근경(랜덤)은 미변경**. 기존 16:9 스테이지는 종전 휴리스틱 그대로. 검증: 텍스처 로드(2520×1080)·FULLFRAME/FIXED 매핑·main.tscn 헤드리스 무에러(실모습 F5/웹). *show_ground_line=false(빨간 정렬선 꺼짐).*
- **✅ 하단 HUD 슬롯 표시 규칙 정리(2026-06-05, v0.0.110)**: `bottom_hud.gd`. ① **동료 슬롯** = 동료 안 껴도 항상 **풀 오퍼시티**(종전 빈칸=0.4 흐림 → 100). ② **스킬 슬롯** = 활성 칸이 **공격버튼에 가까운 "오른쪽 nslots개"만 풀 오퍼시티**, 나머지는 **opacity 0(완전 숨김)**(종전 왼쪽부터·비활성 0.28 흐림 폐기). `nslots=GameState.skill_slots`(길냥이/base=0 → 4칸 다 숨김 / 등급1·2=2칸 / 3·4=3칸 / 5=4칸, 기획 2-2-3-3-4). **빈 활성 슬롯도 풀 프레임 표시**(스킬 끼우면 그 칸에 아이콘 — 아이콘 에셋 추후, 현재 짧은이름 텍스트). 등급↑로 슬롯이 오른쪽부터 왼쪽으로 늘어남. 검증: main.tscn 헤드리스 무에러. *실제 모습 F5 확인.*
- **✅ 치즈 탄환도 일시정지 중 계속 비행(2026-06-05, v0.0.109)**: 전투 중 펑거스 대화 팝업(짧은 트리 일시정지) 때 **치즈(플레이어) 탄환만 허공에 멈춰** 있던 것 수정. `enemy_bullet`은 이미 ALWAYS였으나 `bullet.gd`(치즈 탄환)는 아니어서 멈춤. → `bullet.gd`를 `PROCESS_MODE_ALWAYS`로(클리어·대화 팝업 등 모든 일시정지 중에도 계속 날아감) + `_on_area_entered`에 **일시정지 중 피격 판정 보류**(통과만, enemy_bullet과 동일 모델: 정지 중엔 전투 미해결·시각적 비행만). 검증: main.tscn 헤드리스 무에러.
- **✅ 타격음 punch.wav 끝 소음 제거 + BGM 진단(2026-06-05, v0.0.108)**: ① **타격음("퍽") 끝 "덜컥" 소음** — `punch.wav` 엔벨로프 분석 결과 0~330ms 펀치+감쇠 뒤 330~480ms 무음, **480~520ms에 뜬금없는 소음 블립**이 있었음(이게 꼬리 소음). **350ms에서 컷 + 끝 12ms 페이드아웃**(클릭 방지)으로 0.52s→0.35s 트림(`assets/sfx/punch.wav` 재임포트). ② **"배경음악 사라짐" = 코드 버그 아님** — 헤드리스 검증(트랙 3종 길이 정상, `bgm_enabled=true`면 `playing=true`)으로 음악 시스템 정상 확인. 원인 = `bgm_enabled`가 **false로 저장됨**(세이브 확인). 이 값을 바꾸는 곳은 **홈 우상단 [설정] 버튼(`Music.toggle()`)뿐** → [설정]을 눌러 BGM이 꺼진 상태. **복구 = [설정] 다시 누르면 "배경음악 켜짐"**. *(설정 버튼이 BGM 토글 전용이라 오인 클릭 시 끄기 쉬움 — 추후 설정 UX 개선 여지.)*
- **✅ 배경 풀블리드(노치) + UI 안전영역 인셋(2026-06-05, v0.0.107)**: iOS 홈화면 PWA(standalone)에서 좌우 검정 띠(노치/안전영역 미커버) 해소. ① **캔버스 풀블리드** — `export_presets` head에 **`viewport-fit=cover`** viewport 메타를 런타임 주입(Godot 기본 메타 덮어쓰기, 순서 무관) + `:root`에 `env(safe-area-inset-*)` → CSS 변수 노출 + body 배경 잉크색(혹시 남는 틈 블렌딩) → **배경 아트가 화면 끝까지(노치 영역까지)** 채움. ② **안전영역 인셋을 UI/조작/바닥에만 적용** — `Layout`이 웹에서 `JavaScriptBridge`로 인셋(CSS px→뷰포트 px 변환)을 읽어 `safe_left/top/right/bottom()` 제공(데스크톱·헤드리스=0이라 무변화, 리사이즈·회전 시 재읽기). 적용처: **하단 버튼행**(`bottom_row` 우측·하단 인셋) / **조작 띠·바닥선**(`band_top` 하단 인셋 → 홈인디케이터 위) / **조이스틱**(좌측 인셋) / **상단 HUD**(`hud._apply_safe_hud`: HP·하트·코인=좌상단, 적수·일시정지=우상단, 스테이지=중앙, 델타추적으로 누적/리사이즈 안전). 결과: **배경=풀블리드, 버튼·HP·바닥은 노치/홈바 안쪽**. 검증: main/start 헤드리스 무에러. *실제 인셋 동작·미세 위치는 iPhone 실기 확인 필요(기종마다 노치 위치 상이) — 어긋나면 값 튜닝.*
- **✅ 펑거스 등장 = 전부 보스 BGM(2026-06-05, v0.0.106)**: v0.0.105(1-1만)를 확장 — **stage_intro의 모든 인트로·아웃트로가 펑거스**(전 EVENTS `name:"펑거스"` + `_spawn_npc`는 항상 FUNGUS)이므로, **펑거스 등장 이벤트 전부 보스 BGM**으로. ① **인트로**: `intro_music` 기본값을 `""`→**`"boss"`** (모든 펑거스 인트로 자동 적용, 1-1의 중복 명시 필드 제거; 필요시 `intro_music:""`로 개별 해제 가능). 전투 시작 큐에서 battle 복귀(기존). ② **아웃트로(전투 후 펑거스 등장 대화)**: `play_outro` 시작 시 `Music.set_override(outro_music 기본 "boss")` → 퇴장 끝에 `clear_override` → 클리어 화면은 battle. 결과: *보스(인트로)→battle(전투)→보스(아웃트로)→battle(클리어)*. **단, 전투 중 펑거스 대사 팝업**(first_kill·on_wave)은 NPC 등장 아닌 짧은 끼어듦이라 **battle 유지**(잦은 전환 방지 — 원하면 추가 가능). 검증: main.tscn 헤드리스 무에러.
- **✅ 1-1 인트로 보스 BGM 연결(2026-06-05, v0.0.105)**: v0.0.104의 Music 오버라이드 API를 실제 적용(테스트). **EVENTS["1-1"]에 `"intro_music": "boss"`** 추가 → `stage_intro._run_intro`가 인트로 시작 시 `Music.set_override(intro_music)` → 전투 전 펑거스 대화 동안 **보스 BGM**. 전투 시작 큐 시점(`game._on_battle_starting`)에서 `Music.clear_override()` → **battle BGM 복귀**(시작 큐와 동시 전환). 안전망: `Music._process`가 현재 씬이 main.tscn이 아니면 오버라이드 자동 해제(메뉴·홈에 보스음악 누수 방지). 다른 스테이지 인트로에도 `intro_music` 한 줄로 적용 가능. 검증: main.tscn 헤드리스 무에러.
- **✅ 연출·전투 5종 보정(2026-06-05, v0.0.104)**: ① **펑거스 밀기 = 뒷걸음질** — 펑거스가 치즈를 왼쪽으로 밀 때 치즈가 왼쪽 보고 걷던 것을 **전투처럼 back(뒷걸음질, 우향)** 으로(`player.event_walk_to(target, backstep=true)` + `_event_backstep`; 좌측 이동 시 `back` 애니·flip 없음). ② **이벤트 중 음악 끊김 방지** — `Music`/그 AudioStreamPlayer를 `PROCESS_MODE_ALWAYS`로(일시정지에도 씬 추적 유지) + `_play_cur`에 **같은 곡이면 재시작 안 함** 가드. **이벤트 전용 BGM 오버라이드 API** 추가(`Music.set_override("boss")`/`clear_override()`, 잘못된 키 무시·즉시 전환) — "첫 등장만 보스음악" 같은 1회성은 호출부 플래그로 가드(현재 미사용, 메커니즘만 준비). ③ **클리어 시 적 탄환(돌) 허공 정지** — `enemy_bullet`을 `PROCESS_MODE_ALWAYS`(일시정지 중에도 비행→바닥 낙하 후 제거) + 일시정지 중엔 피격 판정 스킵(시각적 낙하만). ④ **원거리 적이 화면 밖에서 정지** — 치즈가 우측 스폰 근처서 버티면 투척쥐가 사거리 진입 즉시 화면 밖에서 멈춰 안 들어오던 것 → `_on_stage()`(우측 가장자리에서 `STAGE_ENTER_MARGIN=100px` 안쪽) 게이트를 이동정지·루프슈터 in_range·일반원거리·교전anim 4곳에 추가 → 최소한 화면 안까지 전진 후 정지·발사. ⑤ **"전투 시작!" 배너 체류 ↑** — 0.7s→1.5s(팝인 0.28·페이드 0.45)로 읽을 시간 확보. 검증: main.tscn 헤드리스 무에러·전 스크립트 구문. *실연출은 F5/웹.*
- **✅ "전투 시작!" 큐 타이밍 개선 — 적 등장 선행(2026-06-05, v0.0.103)**: v0.0.102는 배너가 첫 적 스폰과 **동시**에 떠 큐가 애매했음(특히 인트로 후 1.5s 빈 정적). 배너를 **"전투 대기 시작" 시점**(적 등장보다 `start_delay`=1.5s 먼저)으로 앞당겨 *전투 시작! → [적 스트림 등장]* 순서로 분리. `spawner`에 **`battle_starting` 신호**(첫 웨이브 대기 진입 1회, `_announced` 가드) 추가 → `game.gd`가 구독해 배너 호출(웨이브 타이밍 핸들러에서 배너 호출은 제거, 별점 타이머는 그대로 첫 스폰 시점). 인트로 있는 스테이지의 누수 방지: `stage_intro._run_intro`가 **`hold_intro()`를 `await`보다 먼저** 호출(한 프레임도 spawner가 안 돌게) → 배너가 대화 전에 새지 않음. 배너(~1.3s)가 그 1.5s 공백을 채워 자연스러움. 검증: main.tscn 헤드리스 무에러.
- **✅ "전투 시작!" 큐 배너(2026-06-05, v0.0.102)**: 인게임 이벤트(대화)→실제 전투 전환을 명확히 하는 시작 큐. `hud.show_battle_start()` 신규 — **잘난체 64px "전투 시작!"**(골든+INK 외곽, design.md display 토큰) 배너가 화면 중앙에 **팝인(BACK 이징 스케일)→0.7s 유지→페이드아웃** 후 자동 제거 + **트럼펫 팡파르**(`Sfx.play("trumpet")` 합성음). `game.gd._on_wave_for_timing`의 **첫 웨이브(current==1)** 지점에서 호출 → intro 유무·스테이지 무관 모든 전투 시작에 1회 표시(별점 타이머 시작과 동일 지점). 검증: main.tscn 헤드리스 무에러 + show_clear와 동일한 `get_viewport().get_visible_rect()` 패턴(실게임 정상). *연출 실확인은 F5/웹.*
- **✅ 프롤로그 순서 변경 — 게임시작 후 재생(2026-06-05, v0.0.101)**: 종전 "부팅 시 프롤로그 자동재생 → 시작화면" 순서를 **"시작화면 먼저 → 새 게임 진입 시 프롤로그 1회 → 홈"** 으로 변경. ① `start.gd`의 **부팅 자동재생(`if not prologue_seen` 딥링크) 제거**. ② **[게임 시작](세이브 없음=새 게임)·[처음부터]** = `reset_progress`/`new_game` 후 `_play_prologue_then_home()`(프롤로그 경유 → 홈). ③ **[이어하기](세이브 있음)** = 로드 후 **프롤로그 없이 바로 홈**. ④ **[이야기] 다시보기** = 프롤로그 후 시작화면 복귀. 라우팅은 `GameState.prologue_return`(임시·비저장: 새게임=`home`/그외=`start`)으로 — `prologue._finish`가 이 값 보고 홈 또는 시작화면으로 이동(소비 후 `start` 복귀). `prologue_seen`은 기록/다시보기용으로만 남고 **자동재생 게이트에서 해제**(게이팅=action 기반: 이어하기 X / 처음부터·첫시작 O). 검증: start·prologue 씬 헤드리스 무에러 로드(멀티라인 람다 포함). *흐름 실확인은 F5 — 첫 실행 게임시작→프롤로그→홈 / 재실행 이어하기→홈(프롤로그 X) / 처음부터→프롤로그.*
- **✅ 점프 중 이벤트 = 착지 후 진행(2026-06-05, v0.0.100)**: 점프(공중) 상태로 마지막 적을 처치하거나 대화 팝업이 뜨면, 트리 일시정지가 **치즈를 공중에 멈춰** 떠 있는 채로 펑거스와 대화하던 어색함 수정. ① `player.is_airborne()` 신규(점프 air 상태거나 발 y가 바닥선 위면 true). ② `stage_intro._await_cat_landed()` — **일시정지 직전**(아직 물리가 도는 동안) 치즈가 공중이면 착지까지 `process_frame` 대기(안전상한 ~3초). 인트로·대화 팝업·클리어 아웃트로 3곳 모두 pause 전에 호출 → 착지 후 정지. ③ `game.gd` 클리어 직접-pause 경로(아웃트로 없는 스테이지 포함)에도 동일 착지 대기 → 공중에서 결과창 안 뜸. 검증: main.tscn 헤드리스 무에러(실제 점프-클리어 타이밍은 F5 확인 권장).
- **✅ 버그·연출 5종 수정(2026-06-05, v0.0.99)**: ① **투척쥐 피격 후 무한 idle 버그** — 루프슈터(투척쥐·거미·벌)는 피격 시 `hit` 애니가 `_update_loop_shooter`의 `_play_move_anim()`에 가로채여 `animation_finished("hit")`가 안 와 `_hit`이 영영 안 풀려 정지하던 문제. **(A)** `enemy.gd`의 피격 경직 해제를 **모든 적 공용 타이머**(`if _hit:` 0.22s, 종전 placeholder 한정 `and not _use_sprite` 제거)로 안전망화 + **(B)** 루프슈터 정지 분기에서 `_hit` 중이면 `_play_move_anim()` 미호출(hit 애니 보존). 다른 적도 동일 안전망 적용됨. ② **인앱 이벤트 중 배경(비) 정지** — 이벤트는 트리 일시정지라 배경도 멈추던 것 → `stage_intro`에 `_set_bg_alive(on)` 헬퍼(BG/Weather/Foreground를 `PROCESS_MODE_ALWAYS`↔INHERIT 토글) 추가, 인트로·팝업·아웃트로 3곳에서 호출 → 이벤트 중에도 비·드리프트·바람 유지(조작은 정지). ③ **클리어 시 죽음/타격 이펙트 정지** — 마지막 적 처치=게임종료 판정으로 트리 일시정지되며 귀신·펑·타격 이펙트가 얼어붙던 것 → `_die`의 적 노드·POP과 `Fx.burst` 스프라이트를 `PROCESS_MODE_ALWAYS`로 → 일시정지 중에도 끝까지 재생 후 사라짐. ④ **클리어 후 펑거스 등장 위치** = 인트로처럼 **화면 우측 가까이**(종전 `vp.x*0.6` 중앙 → 인트로 standing 재현). ⑤ **펑거스가 치즈 통과/추월 금지** — 침입자 "치즈 겹침 불가" 규칙 적용. **치즈=펑거스 왼쪽**(사용자 확정), 펑거스가 우측에서 들어와 치즈를 지나치지 않고 그 오른쪽에 정지. 치즈가 너무 우측이면 `player.event_walk_to()`로 **왼쪽으로 밀어** 대화 간격(240px) 확보. 검증: main.tscn 헤드리스 무에러 로드(②③④⑤는 일시정지·물리 연동이라 실기 F5/웹 최종 확인 권장).
- **✅ 컷씬 다중 페이지 플레이스홀더 + 대사 전부(2026-06-05, v0.0.98)**: 인게임 이벤트 컷씬을 **대본 장 수에 맞춘 다중 페이지**로 확장 + 대본 대사 전량 삽입. `game.gd`에 페이지 데이터 상수(`CUT_SHERIFF` 4장 / `CUT_PEARL` 6 / `CUT_MAX` 7 / `CUT_DOVE` 4 / `CUT_CHIHUAHUA` 3 / `CUT_GAMEOVER` 3) + `_play_cutscene(pages)`·`_cut_page(img,line,idx,total)` 신규. **각 페이지 = 회색 이미지 플레이스홀더("[그림] + 그릴 그림 설명") + 금색 대사 + 페이지수(예 4/6) + 탭**. 실제 아트 없이 "어떤 그림인지+무슨 대사인지"만 표기(추후 일러스트 교체). 기존 단일 `_play_placeholder_cutscene` 폐기, 1-3/1-5/1-7/1-13/1-16/게임오버 전부 새 시스템. *(프롤로그는 별도 `prologue.gd` 6장면+골드 대사 이미 완비 — 미변경 / 막전환 1-20은 보스 미정이라 보류.)* 검증: 잔존 호출 0·main.tscn 무에러·페이지 실기 렌더(회색박스+그림설명+대사+4/6) 확인.
- **✅ 1-5 펑거스 박쥐 소개 인트로 추가(2026-06-05, v0.0.97)**: 대본 갱신(1-5에 "① 전투 전 — 펑거스 박쥐 소개" 신설, 기존 섹션 ②③④⑤로 재번호) 반영. `stage_intro.gd` EVENTS에 **`"1-5"` 인트로**(케이스 0, 펑거스 2줄 "오늘은 하늘이다…"/"머리 위는 어쩔 테냐") 추가 — 1-11/12/14와 동일 구조. **펄 해금(케이스 A 인스씬, 클리어 후)과 시간상 분리돼 공존**(`has_outro=false`라 펄 인스씬 정상 진행, INSCENE 5 유지). 검증: 키 "1-5" 픽업·인트로 2줄·has_outro false·INSCENE 5 포함, main.tscn 무에러. 문서=`기획_이벤트대본_1막.md` 1-5 갱신.
- **✅ 적별 걷·멈 리듬 차등 def화 — 초반 텐포(2026-06-05, v0.0.96)**: 지상 적이 전부 같은 기본값(`walk_time 1.5`/`stop_time 1.0`)으로 똑같이 걷고-멈춰 초반이 단조하던 문제. `enemy.gd._apply_def()`가 def의 **`wt`/`st`를 읽도록** 2줄 추가(없으면 @export 기본 유지, 이후 `_ready`의 randf 곱셈이 적용돼 변동 유지). `enemy_data.gd` 4종에 필드 추가: **회색쥐 1.4/0.6**(멈춤 유지·깨작↓) / **회색롤러쥐·검은롤러쥐 6.0/0.15**(거의 안 멈춤=돌격 정체성 복원→몸으로 못 밀고 사격강제) / **검은쥐 2.0/0.7**(덜 멈춰 압박↑). 투척쥐·거미·공중·보스는 미변경(기본값/별도 로직). **정합**: `시스템밸런스 §3.1` 표 + `침입자도감` 4종 걷·멈 리듬 줄 코드값으로 갱신(검은쥐 멈 0.8→0.7 불일치도 정정). 검증: def_of 출력(4종만 wt/st)·main.tscn 무에러. *이속·HP·DMG·웨이브 미변경(리듬만).*
- **✅ 부트 스플래시 = 치즈 + "HOME PURRTECT"(2026-06-05, v0.0.90)**: 로딩 시 뜨던 **Godot 기본 로봇·"Game engine" 텍스트 → 자체 스플래시로 교체**(Godot은 MIT라 로고 표시 의무 없음). `assets/ui/splash.png` 생성(치즈 idle + Jalnan "HOME PURRTECT", 크림 배경) + `project.godot boot_splash/image`·`bg_color` 설정. 웹 로딩 화면도 이 스플래시 사용. *(임시 — 나중에 로딩 전용 일러스트로 교체 가능.)*
- **✅ 웹 로딩 속도 최적화 — 텍스처 손실압축(2026-06-04, v0.0.89)**: "웹 로딩 느림/게임 중 뜬금 로딩" 원인 = **텍스처가 전부 무손실(`compress/mode=0`)로 import**돼 배포 에셋 175MB(텍스처만 151MB) + Godot WASM 30MB. (※ `music/library` 112MB는 `.gdignore`로 이미 제외 — 범인 아님.) **수정**: 스프라이트·fx·배경 1177장을 **손실 압축(`compress/mode=1`·품질 0.85)**으로 전환(아이콘·UI는 무손실 유지) → **텍스처 151MB→63MB, 전체 imported 175MB→86MB(~절반)**. 실기 스크린샷으로 화질 이상 없음 확인(카툰 아트는 0.85로 충분). + 미사용 placeholder `test2/3.png` 삭제. *게임 중 멈칫(스테이지별 lazy load·셰이더 첫 컴파일)은 다음 단계(스테이지 진입 시 프리로드).* **배경 재제작(21:9) 시엔 처음부터 손실 압축으로 import할 것.**
- **✅ 웹 로딩 404 수정 + 클리어 보상 아이콘 크기 수정(2026-06-04, v0.0.88)**: ① **웹 `Failed loading 'index.<sha>.pck'`** — CI가 `.pck/wasm/js`를 커밋 SHA로 리네임하던 캐시버스트가 **연속 배포 시 404 유발**(GitHub Pages 전체교체 배포라 옛 SHA 파일 삭제 → 캐시된 옛 index.html이 사라진 pck 요청). → **안정 파일명으로 전환**(리네임 폐기, ETag 재검증). 404 구조적으로 제거. *(현재 깨진 캐시는 1회 강력 새로고침 필요.)* ② **클리어 보상 박스 아이콘이 슬롯보다 큼** — `TextureRect.expand_mode` 기본값(KEEP_SIZE)이 최소크기=원본픽셀(188px 등)이라 88px 칸을 뚫음 → **`EXPAND_IGNORE_SIZE`**(컨트롤 크기에 맞춰 축소) + 타일 `clip_contents`. 실기 스크린샷 확인.
- **✅ 문서 전수 대조·정합 갱신(2026-06-04, v0.0.87)**: 코드(SSOT)와 19개 문서 전수 audit(병렬 4에이전트). **데이터(도감5·밸런스·호감도·가격·드랍·스탯)는 드리프트 0건**(부보안관 19.5↔20은 표시 반올림뿐). stale했던 "구현 여부" 주장만 코드 실제로 교정: ① **§1 한눈에 보기** "메타 시스템 미착수"→"로드맵 1~6단계·침입자 스프라이트·별점·세이브 완료, 남은 건 보스·M배율·아트·웹세이브검증". ② **§2/§4-A 적 스프라이트** "8종 placeholder"→"침입자 10종 진짜 스프라이트 완료, 보스 2종만 placeholder"(`enemy.gd` 헤더 주석도 교정). ③ **§4-B 등급** `[코드-기획 불일치]`·"리팩터 대기" 삭제 → **합성 승급(직전 소모) 이미 구현 확인**(`craft_grade`=`_remove_grade(직전)`+재료+코인). `자산관리_전투준비.md` ⚠️경고도 ✅로. ④ **§4-D 홈** "[ ] 테스트용 시작 선택"→완료. ⑤ **§5 코드 위치** 누락 스크립트 추가(stagemap·game·pearl·npc_talk·stage_intro·prologue·배경3종·style·sfx·music). ⑥ **§6 버전사** X.Y.Z 마일스톤 한 줄 요약 추가(상세는 상단 ✅로그). **소소 TODO(미수정)**: `select.gd` ITEM_ICON에 폭탄·후추·쥐덫 아이콘 미연결(소모품 표시 placeholder).
- **✅ 진행 영속화 (세이브 ON) — 마일스톤 M1(2026-06-04, v0.0.86)**: 웹에서 새로고침하면 1-1로 초기화되던 것 해결(별점·맵 등 최근 시스템이 웹에선 휘발되던 근본 문제). ① **`AUTOSAVE = true`**(플레이어 모드만 저장, 개발자 모드는 격리). ② **부팅 로드**(`GameState._ready`에서 `load_game()`) → 시작화면이 코인·`prologue_seen` 등 올바른 상태 반영(프롤로그 무한 재생 방지). ③ **[게임 시작]→[이어하기]**: 세이브 있으면 로드(=개발자 세션 잔여 해금/코인 제거, 세이브가 진실원천) / 없으면 새 게임. ④ **[처음부터]** 버튼(세이브 있을 때만 노출) + **확인 팝업** → `new_game()`(진행·별점·올스타·코치마크 초기화 + 세이브 덮어쓰기, 설정·프롤로그본여부 유지). `has_save()`/`new_game()` 신규. 헤드리스 검증: 라운드트립(coins1234·stage7·cleared6·stars·materials 복원) / 새게임 초기화 / 재로드 0·1 / cleanup. **남은 것(M1-⑤)**: 웹 IndexedDB 영속화는 배포 후 새로고침으로 실검증.
- **✅ DEV 도구 노출을 개발자 루트로 한정(2026-06-04, v0.0.85)**: 홈 [DEV 도구] 버튼이 `is_dev()`만 보고 떠서 **일반 [게임 시작]→홈(mode=player)에서도 노출**되던 것 → **`mode == "dev"`일 때만** 노출(`home.gd`). 루트 분리 명확화: **일반** = 시작→[게임 시작]→홈(DEV 도구 숨김) / **개발자** = 시작 우하단 개발자 버튼→개발자 메뉴→[홈 화면으로](mode=dev, DEV 도구 노출) 또는 [이 설정으로 시작](전투). 헤드리스 검증(player 홈=버튼 없음 / dev 홈=버튼 있음).
- **✅ 개발자 메뉴 [홈 화면] 접근성 수정(2026-06-04, v0.0.84)**: "개발자 모드 = 전투 스테이지만 보인다"는 제보 원인 = 메뉴가 세로로 길어(콘텐츠 ~855px) 작은 화면서 **하단 [홈 화면]·[뒤로]가 잘려 안 보임** + 회색이라 큰 빨강 [이 설정으로 시작]에 묻힘. ① **`ScrollContainer`로 감싸 세로 스크롤** → 어떤 화면비에서도 전부 도달. ② [홈 화면으로 (개발 도구)] 버튼을 **골드(브랜드)로 강조** + "— 또는 —" 구분선으로 "전투(빨강)와 별개 목적지"임을 명확히. (홈 진입 → 홈 [DEV 도구]로 코인·스테이지·전리품·상점 등 메타 화면 점검 가능 — 기능 자체는 v0.0.75부터 있었음, 도달성만 개선.) 실기 스크린샷 확인.
- **✅ 클리어 결과 화면 개편 — 쿠키런 톤(2026-06-04, v0.0.83)**: 텍스트 한 덩어리였던 클리어 패널을 비주얼 결과창으로. ① **상단 큰 `CLEAR!`**(골든+잉크 외곽 64px) + **별 3개 그래픽**(`StarRow` 커스텀 _draw, 획득=골든 채움/미획득=빈 크림, 가운데 별 크게) + 클리어 시간(MM:SS)·다음 목표시간 한 줄. ② **하단 보상 박스**: 크림 패널(물건화) 안 `HFlowContainer` 타일 — **코인(이번 판 획득)** + **전리품 전부** 아이콘+개수(88px 타일·잉크 외곽). 아이콘=`assets/items/loot/{id}.png`(예외 4종 `sparrow_feather→feather`·`spider_silk→cobweb`·`wheel→steel_wheel`·`sack→loot_sack`, 출처 아이템도감). ③ 결과창 뜰 때 **전투 HUD(체력·조이스틱·하단버튼) 자동 숨김** → 깨끗한 화면. ④ **이번 판 코인 집계**(`GameState.run_coins`, `add_coins()`로 처치+첫클리어 보너스 누적) 신규. design.md 토큰 준수(타일=card_box·CTA 빨강·골든 별). 검증: 빌드 스모크(자식 7·타일 6·아이콘 별칭 해소 무에러) + **실기 스크린샷 렌더 확인**.
- **✅ 개발자 모드 GUI 가시성 수정(2026-06-04, v0.0.82)**: 전역 테마(`caldera_theme.tres`)가 **밝은 톤**인데 `CheckBox`·`OptionButton`·`SpinBox`는 타입별 글자색을 안 정해둬서 **엔진 기본=흰 글씨 → 밝은 바탕에 안 보이던** 문제. ① **개발자 메뉴**(`dev_menu.gd`): `_font` 헬퍼가 글자색 **INK 강제**(무적/즉사/스킬·동료 지급 체크박스, 직업 OptionButton, 스테이지 SpinBox 전부 가독) + 안내문(`_dim`) 회색 0.45→0.32(대비↑). ② **인게임 🐞 디버그 오버레이**(`debug_overlay.gd`): 섹션 헤더 **골드(크림 위 저대비)→빨강**(§14), `_font` INK 강제, 스테이지 SpinBox에도 적용. ③ 홈 [DEV 도구] 패널은 이미 Design(INK on PAPER)이라 정상. 헤드리스 검증(dev_menu 로드·dev모드 main에서 오버레이 빌드 무에러).
- **✅ 스테이지 맵 design.md 정합 교정(2026-06-04, v0.0.81)**: 신규 맵 화면을 design.md §7 체크리스트로 크로스체크 → 위반 5건 수정. ① **보스 칸 강조** 골드 채움→**빨강 잉크 테두리**(골드=획득 전용·보스=전투 빨강 §1, 골드 남용·"강조 1개" 해소). ② **상단 코인·별총합** 맨 라벨→**재화 캡슐**(framed_plate, "그릇 안에" §3.5 #1·#6, 글자=잉크/크림 위 골드 금지 §14). ③ **컬렉션 보상 칸** 맨 둥근사각→**물건화**(모서리 볼트4 §0-7) + 위계 정리(구간/보석명/★진행). ④ **버튼 높이** 48→**BAR_H 52**(§3.5 #2) + 8px 그리드 정합(gap14→16·band150→144·top76→80). ⑤ **토스트** 맨 골드텍스트→**PAPER 알약·상단중앙**(§3.6). + 홈 [지도] 버튼 골드→크림(내비=Secondary, 출격 빨강 강조 1개 유지). 헤드리스 재검증(home·stagemap 무에러·맵 빌드 29). *(남은 권장: 여백 만화기호 장식 §0-5 — 추후.)*
- **✅ ★ 스테이지 맵(별점 허브) + 클리어 팝업 별 표시(2026-06-04, v0.0.80)**: 별점 데이터를 "보여주고 수령·재도전하는 UI" 신규. ① **클리어 결과 팝업 별 연출**(`hud.show_clear`): ★★★ 채움/빈칸(`★··`) + (★N/3) + **클리어 시간 MM:SS** + **다음 목표시간**("★★ 120s · ★★★ 80s") + 재도전 시 **신기록!** 한 줄. (별 톡톡 채우는 애니는 클리어 패널이 단일 Label이라 즉시표시로 — 스펙 허용.) ② **스테이지 맵 화면**(`scenes/stagemap.tscn`·`stagemap.gd`, 홈 좌상단 **[지도 ★N/60]** 버튼으로 진입 — 기존 placeholder 대체): 상단바([← 홈]·막이름·★총합/60·코인) / **5×4 그리드 1-1~1-20**(cleared만 활성, best_star ★표시, 미클리어=잠금 흐림, **보스 1-10·1-20=빨강 테두리+BOSS 태그**(v0.0.81), 탭=재도전) / **컬렉션 보상 띠 3칸**(구간 올스타 — 진행 N/10·N/20, 상태 3종 [받기]/받음✓/흐림). design.md 토큰만 사용. ③ **재도전=파밍**(`GameState.begin_replay`/`finish_replay`+`replaying` 임시플래그·`frontier_stage`): 진행 포인터를 덮지 않고 그 스테이지만 한 판 → 클리어 시 진행 안 올리고 [지도로]/[다시 도전], 이벤트 컷씬 생략. ④ **구간 올스타 = 수동 수령으로 변경**: 종전 클리어 시 자동지급(`check_allstar`)을 폐기하고 **맵 [받기]**(`claim_allstar`: 보석+1·`allstar_claimed` 기록·중복방지)로 이전(스펙 B "수령=버튼식"). 클리어 팝업의 올스타 줄도 제거. **게이팅 없음**(BM). 헤드리스 검증: 3씬 로드 무에러 / frontier=13·begin/finish 복원 / seg progress(10,10)·can_claim·claim(자수정+1)·재수령'' 차단 / 목표시간(1-9=120/80·보스=180/120) / 맵 빌드(29 children). *(BG·치즈 일러스트는 placeholder, 컬렉션 수령 연출=토스트.)*
- **✅ ★ 스테이지 별점 시스템(2026-06-04, v0.0.79)**: 시스템밸런스 §5.7 구현. ① **★1~3 판정 = 클리어 "전투 실시간"**(`game._battle_time`, 첫 웨이브 스폰~클리어 확정. 일시정지·인게임 이벤트·컷씬은 트리 일시정지라 `game._process` 안 돌아 자동 제외). 기준: 일반=웨이브수×30s(★2)/20s(★3)·보스(1-10·1-20)=180/120s(`rate_stars`). **게이팅 없음**(클리어=무조건 ★1 이상, 다음 스테이지 진행). ② **첫 클리어 보상 별 차등**(첫 클리어 시점 1회 고정, 재도전 추가지급 X): **코인** base(`20+5×N`+보스 +100/+200)에 ★2 ×1.5/★3 ×2 배수(`award_stage_clear(stars)`, `FIRST_CLEAR_STAR2_MULT`/`STAR3_MULT` 상수) + **보석** ★2=하급1·★3=중급1 랜덤(`first_clear_star_gem`). 코인 검증표 20스테이지 전부 일치(1-1 25/38/50 … 1-20 320/480/640). ③ **best_star 재도전 max 갱신**(`stage_stars` 세이브) → ④ **구간 올스타**(1-1~10/11~20/전체 전부★3) 각 1회 자수정·로즈쿼츠·사파이어(`check_allstar`·`allstar_claimed`). ⑤ 전투 HUD 상단 경과시간 라벨 / 클리어 결과창 ★·시간·획득 보석 표시(`hud.show_clear(bonus, star_info)`) / 홈 좌상단 ★N/60 진행도(`allstar_progress`). 세이브 마이그레이션=옛 세이브 빈값 초기화. 헤드리스 검증(임계·판정·보석 랜덤·올스타 지급/중복방지·진행도·전체 클리어 흐름·코인표 0불일치). *(per-stage 별 표시·올스타 수령은 v0.0.80 스테이지 맵에서 — 위 항목 참조. v0.0.80에서 올스타 자동지급→맵 수동 [받기]로 변경.)*
- **✅ 검은쥐 셰이더 = 털만 어둡게 + 돌멩이 밝게(2026-06-04, v0.0.77)**: 검은쥐 리스킨이 롤러스케이트·헬멧·포대까지 다 까매지던 것 → **채도 기반** 수정(`enemy_darken.gdshader`: 채도 낮은 회색 털만 어둡게, 채도 높은 분홍 헬멧·스케이트·갈색 포대 + 흰 손·눈 보존. `sat_keep 0.20`/`white_keep 0.74`). PIL 시뮬로 롤러쥐·투척쥐 장비 보존 확인. + **투척쥐 돌멩이** 너무 어둡던 것 밝고 따뜻한 회색으로(body 0.55→0.68).
- **✅ 1막 비-보스 이벤트 전부 구현(2026-06-04, v0.0.76, 하트 제외)**: 대본 확장본 일괄 반영. ① **펑거스 약올리기 인트로(케이스 0)**: 1-11 흑화(검은쥐)·1-12 거미·1-14 벌 — `stage_intro` EVENTS에 intro만(표정 angry/laugh). ② **구출형 인스씬 컷씬(케이스 A)**: 1-13 비둘기·1-16 치와와 — 플레이스홀더(원) 전장 상주 + 치즈가 다가가 줌인→컷씬(`game._event_rescue`). `INSCENE_EVENT_STAGES=[3,5,7,13,16]`. ③ **알림 라우팅(케이스 D/D+)**: 결과창 하단에 **✨ 해금 알림 한 줄**(드랍과 분리). 1-15·1-19=D(알림만, 다음▶) / 1-9=D+(알림+[확인]→홈→**스킬 코치마크**, `HOME_EVENT_STAGES=[9]`). ④ **홈 코치마크 추가**: skill(1-9, 맥스 재강조)·dove(1-13, 전투준비>동료). ⑤ **게임오버 컷씬**: 영감 전보 3페이지 플레이스홀더 → 같은 스테이지 재도전(자산 유지). 보스(1-10·1-20)는 추후. 헤드리스 검증(인트로 14·구출13 컷씬·게임오버 컷씬·라우팅 15/9/13/2). *(컷씬 아트·동료/펄/맥스 스프라이트는 추후 — 현재 텍스트 플레이스홀더.)*
- **✅ 개발 모드에서 홈 화면 + DEV 도구(2026-06-04, v0.0.75)**: 개발자 모드로 전투뿐 아니라 **홈도 테스트** 가능. ① 개발 메뉴에 **[홈 화면 (개발 도구)]** 버튼(`_on_home` — mode=dev 유지하고 home으로). ② home `_ready`가 dev 모드면 player로 안 덮음. ③ **홈 좌상단 [DEV 도구] 버튼**(is_dev 전용) → 패널: **코인 +1만/+10만/+100만**(즉시 라벨 갱신) · **스테이지 ◀이전/다음▶/막끝**(`_dev_set_stage`=stage_minor 설정+직전까지 cleared 처리+해금 보강+홈 리빌드 → 버튼 노출·표기 갱신) · **전리품·보석 전부 +20** · **소모품 전부 +10** · **직업·스킬·펄 전부 해금**. 헤드리스 검증(코인·전리품·소모품·해금·set_stage8→펄/맥스/전투준비 노출).
- **✅ 홈 레이아웃 레퍼런스 개편(2026-06-04, v0.0.74)**: 쿠키런류 레퍼런스 배치로 재구성(스타일은 design.md 토큰 유지). **좌상단**=`N막 N스테이지`(framed_plate) / **우상단**=종·편지·친구·설정·메뉴 5버튼(84px 균일·비겹침, 설정=BGM·메뉴=가방·나머지 준비중) + **그 왼쪽 보유 코인** / **좌측 중앙**=펄·맥스 세로 스택(게이팅) / **좌하단**=전투 준비(secondary, "내 쿠키들" 위치) / **우하단**=출격(cta 빨강, PLAY 위치) / **중앙**=치즈 ×2.1 / **배경**=실내 플레이스홀더(벽·바닥·러그 2톤). 코치마크·게이팅은 v0.0.73 유지(rect만 새 위치). 좌표 검증으로 레퍼런스 대조. *(BG 실제 이미지·펄/맥스 최종 아트는 추후.)*
- **✅ 홈 개편 — 해금 게이팅 버튼 + 코치마크(2026-06-04, v0.0.73)**: 가이드 §1-B "해금 후 홈 코치마크" 반영. ① **좌하단 버튼 스택**(좌정렬): 위 = **펄·맥스 나란히** / 아래 = **전투 준비**. 펄/맥스 일반(paper) 버튼 룩. ② **해금 게이팅** — 각 버튼은 해당 스테이지 클리어 후에만 노출(`cleared_stages`: 전투준비=1-3 / 펄=1-5 / 맥스=1-7). 그 전엔 없음. ③ **코치마크**(`assets/shaders/coachmark.gdshader`): 전체 딤 + **버튼 위치만 원형 마스킹**·강조 링·안내 문구. 클리어 직후 첫 홈에서 해금 버튼 1개 안내(전투준비→펄→맥스 순), **1회성**(`coachmark_seen` 세이브, 탭 시 닫힘). 헤드리스 검증(미클리어=없음 / 1-3만=전투준비+코치 / 이미봄=코치 안뜸 / 3종=펄·맥스·전투준비). *(BG 실내·펄/맥스 최종 디자인은 추후 — 현재 기존 BG + paper 버튼.)*
- **✅ 통일 죽음 귀신 + 공중/탄환 그림자 높이연동(2026-06-04, v0.66)**: ① **공중 적 그림자** = 치즈 점프와 동일 로직으로 진동 높이 연동(`1-_air_raise()/500` → 높을수록 작고 옅게). ② **적 탄환 그림자**(`enemy_bullet`) = 바닥과의 높이로 크기·농도 변화(회전 보정). ③ **죽음 귀신 통일**: 어떤 적이 죽어도 동일한 흰 귀신(`assets/sprites/ghost/ghost_frames.tres`, 24프레임 float 루프, 1280×720→크롭 300px). 검은쥐 셰이더 제거하고 흰색. ④ **귀신 모션**: 그 자리에서 1.1s간 위로 130px 떠오르며 알파 1→0 페이드(create_tween) 후 free. ⑤ **귀신 크기**=적 크기 비례(`_body_r×2.4×_size_mult`, 거미 0.63>쥐 0.44>벌 0.39). 헤드리스 검증(귀신 1.13s 후 free, 탄환 그림자 스테이지 무에러).
- **✅ 공중 적 지상근접 가능(높이연동) + 발사 부위 정확(2026-06-04, v0.64)**: ① 치즈 지상 근접이 공중 적을 **무조건 스킵(점프 강제)** 하던 것 → **현재 진동 높이 연동**(`air_height_now()` ≤ `MELEE_AIR_REACH=150`이면 타격, 그 위는 점프 필요). 참새가 낮게 내려오면 점프 없이 타격됨. (검증: 최저점 90px=맞음 / 최고점 350px=안맞음.) ② **탄환 생성 위치를 발사 프레임의 신체 부위로**(`EMIT_OFFSET`, 스프라이트 분석): 투척쥐=던지는 손, 벌=엉덩이 침, 거미=복부, 박쥐=입. `_emit_pos()`가 스프라이트 중심+부위오프셋×스케일(공중은 진동높이 반영). 박쥐 음파도 입 높이에서 발사. **(v0.65: 거미 발사 프레임 #3→#9 — 엉덩이가 좌측을 완전히 향하는 후반, 발사점=엉덩이 뾰족한 끝 (−115,30)px.)**
- **✅ 침입자 공격 프레임동기 + 공중 상하진동(2026-06-04, v0.63)**: ① **프레임 정확 발사/타격** — attack 애니의 실제 release 프레임(`ATK_RELEASE`: 투척8·벌7·거미3·박쥐4·참새6)에 발사/타격 동기(`_resolve_pending_release`). ② **루프 슈터 일반화**(`LOOP_SHOOTERS`=투척·거미·벌): 사거리 안에서 attack 사이클 네이티브 루프 + release마다 발사, 속도=공격간격. ③ **공중 3종 상하진동**(`AIR_BOB`, 개체별 위상 분산): 박쥐 폭120(최대)·벌 폭40(빠름)·참새 폭130. 히트박스도 진동 높이 추종, 효과/팝업도 `_air_raise()`로 실시간 높이. ④ **박쥐**=진동 최저점(=치즈 얼굴 높이 −180)에서만 음파 발사(`_update_air_attacker`, 공격 중 진동 정지). ⑤ **참새**=최저점(치즈 근처)에서만 쪼기. 옛 `_dive`/고정 `AIR_HEIGHT` 띄움 폐기. `--fixed-fps 60` 검증: 진동 폭(박쥐240/벌80/참새260)·박쥐 최저−180·발사수, 실플레이어 근접/원거리/음파/쪼기 데미지 모두 정상.
- **✅ 투척쥐 던지기 사이클 루프(2026-06-04, v0.62, v0.61 짧은재생 폐기)**: 사거리 안에서 멈춰 연속공격하는 투척쥐는 발사 사이에 **idle 대신 "자루에서 돌 꺼내는 준비동작"이 자연스럽다**는 피드백 반영. attack 전체 사이클(줍기#0~5→던지기#6~10→마무리#11~14)을 **네이티브 루프**(SpriteFrames 복제 후 `set_animation_loop("attack",true)`)로 돌리고, **발사는 #8(release) 프레임 통과 순간**(`_update_thrower`). 루프 속도(`anim.speed_scale`)를 atkint에 맞춰 **사이클 길이 = 공격 간격**. `--fixed-fps 60` 검증: gray_thrower 6초 4발(atkint 1.4)·black_thrower 5발(1.1), 기대치 일치. (`THROW_WINDOW`는 release 프레임 정의에만 사용.)
- **✅ 침입자 크기/타이밍 2차 튜닝(2026-06-04, v0.60)**: ① **참새 크기** 1.32→**1.16**(원본↔이전 중간). ② **참새 쪼기 타이밍** — 급강하 중엔 walk/flap 유지, **최저점에서 attack(쪼기) 모션+타격** 동기화(이전엔 내려오며 쪼는 모션 어긋남). ③ **원거리 발사 동기화** — 와인드업을 **실제 attack 애니 길이 기반**(`_attack_release_time`, 발사=모션 55%·근접=50% 지점)으로, 발사가 모션 중에 나오게. 와인드업 중 위상토글이 모션 끊지 않게 가드. 교전 중(사거리 안)엔 발사 사이 **idle 유지**(walk 제자리걸음 제거). ④ **검은쥐 3종 크기** ≈ **회색투척쥐**로 축소(black 1.05·black_roller 1.18·black_thrower 0.93 → scale ~0.56). ⑤ **그림자 크기 `_size_mult` 반영**(거미·투척쥐 등 큰 적은 그림자도 크게). 헤드리스 검증(scale/그림자 + 16스테이지 무에러).
- **✅ 침입자 타이밍·크기·다이브 정리(2026-06-04, v0.59)**: ① **공격 와인드업** — 모든 적이 attack 모션을 **먼저 재생**하고 일정 시간 뒤 실제 타격/발사(`_start_attack`→`_resolve_attack`, 근접 0.22s·원거리 0.30s). 모션 전에 데미지/탄환이 나오던 문제 해결. 와인드업 중 죽음/스턴/경직되면 공격 취소. ② **참새 급강하** — 스프라이트도 `_dive` 동안 바닥까지 내려갔다 떠오르도록 `anim.position.y` 반영(타격은 최저점 `DIVE_DUR*0.5`에서). ③ **거미** 원본 보라 색감 유지 확인(프레임 있는 적은 곱연산 다크 제외 — v0.58 가드). ④ **거미 발 위치** `FOOT_NUDGE`로 ~29px 내려 지면에 밀착. ⑤ **크기 배율 `SIZE_MULT`**(쥐·회색투척쥐 제외 키움): 롤러 ×1.28·검은쥐 ×1.18·검은투척 ×1.12·박쥐 ×1.35·벌 ×1.40·참새 ×1.32·거미 ×1.32. 헤드리스 검증(전 적 scale/material/발y + 16스테이지 1600프레임 무에러).
- **✅ 검은쥐 리스킨 셰이더 + 탄환 외형(2026-06-04, v0.58)**: ① **검은쥐 3종 셰이더 리스킨**(`assets/shaders/enemy_darken.gdshader`) — 곱연산 modulate(흰 손·눈까지 검정) 폐기, **밝기 기반**으로 회색 몸통만 어둡게·흰 부분 보존(`darken 0.40`/`white_keep 0.74`). `_base_modulate`는 흰색 유지(피격 플래시 정상). ② 검은쥐 3종 스프라이트 **×1.12 크게**(회색보다 큼). ③ **투척쥐 2종 탄환 = 길냥이 돌멩이**(enemy_bullet `shape="stone"`, 회색 돌+회전, cat `bullet.gd` 동일 드로잉 이식). ④ **벌 탄환 = 원뿔 독침**(`shape="cone"`, 진행방향 정렬 삼각 침). 헤드리스 스모크로 셰이더 적용·크기·탄환 shape 검증.
- **✅ 침입자 7종 + 펑거스 실제 스프라이트(2026-06-04, v0.57)**: 침입자 7종(회색쥐·롤러·투척 / 박쥐·벌·참새·거미) 원본 프레임 크롭·정규화(캐릭터 바운딩 ~300px) + `<id>_frames.tres` 자동생성. 검은 계열 3종은 회색 프레임 **어둡게 재사용**(modulate). `enemy.gd`: `ENEMY_FRAMES` 맵으로 침입자별 SpriteFrames 로드 + 몸크기 기준 자동 스케일·발 오프셋, **walk/정지=idle(없으면 walk)/attack/hit** 애니, ghost 없으면 hit로 사망, **dead 가드**(이동/공격 정지). 공중 유닛(박쥐·벌·참새)=idle 없음→walk. 펑거스(idle/walk/run/angry/fear/laugh) → `stage_intro` NPC를 회색쥐 대역에서 **실제 펑거스로 교체**(등장=walk·정지=idle·퇴장=run, 초상화=fungus idle).
- **✅ 1-1·1-3 인게임 이벤트 코드(2026-06-04, v0.57, 대본 `기획_이벤트대본_1막.md` 충실)**: `stage_intro.gd` EVENTS에 1-1·1-3 4비트 구조 — ① **인트로**(펑거스 등장 대사) ② **첫 처치 팝업**(1-1) ③ **웨이브 시작 팝업**(`on_wave[N]`, spawner `wave_started` 구독: 1-1 W2 / 1-3 W2·W3) ④ **전 웨이브 클리어 후 퇴장**(`play_outro()` — NPC 재등장→분해서 대사→달려 퇴장, `game.gd`가 클리어 패널 **직전 await**). 1-3 ⑤ **보안관 획득 안내 팝업**(`reward`). 비트별 이름 override(향후 `?`→이름 전환 지원). 헤드리스 스모크로 인트로·W2·W3 팝업·outro+보상 전 경로 무에러 검증. **(v0.0.67에서 ⓐⓑ 구현 — 아래 항목 참조.)** **(v0.0.68에서 ⓒ 골격 구현 — 전투 씬 내 궤짝 도보+줌+컷씬(플레이스홀더). 실제 변신 컷씬 아트만 추후.)**
- **✅ 1-5 펄 · 1-7 맥스 해금 인스씬 이벤트 + 1-3 ③ 대사(2026-06-04, v0.0.72, 케이스 A)**: 가이드 §1-B(드랍↔사건 분리) + 대본 1-5·1-7 확정 반영. ① **공통 골격**(`game.gd`): 클리어 → 드랍 결과팝업 [확인] → 씬전환 없이 인스씬 이벤트 → 화면 줌인 → 컷씬 플레이스홀더 → 홈. `INSCENE_EVENT_STAGES=[3,5,7]`. ② **1-5 펄**(박쥐 첫 등장): 클리어 시 **💎빛나는 조약돌 1개 고정 지급**(헌납 튜토). **펄 플레이스홀더(분홍 원)** = 좌상단(2층 창가) 상주. 치즈 좌측 도보 → 펄 줌인 → 컷씬("펄의 축복 해금"). ③ **1-7 맥스**: **맥스 플레이스홀더(갈색 원)** = 담벼락(우중앙) 상주. 클리어 후 맥스가 치즈에게 다가옴 → 줌인 → 컷씬("상점·메이드·음악가 제작 해금"). ④ **1-3 ③ 대사** 대본 갱신본 2줄로 교체. **해금은 stage_minor 진행으로 자동**(펄≥6·메이드/음악가≥8), 컷씬은 연출만. 헤드리스 검증(1-3/1-5/1-7 컷씬 도달·플레이스홀더 상주·1-5 보석 +1). *(컷씬 아트·펄/맥스 스프라이트는 추후 — 현재 동그라미.)*
- **✅ 1-3 보안관 획득 = 전투 씬 내 궤짝 컷씬(2026-06-04, v0.0.68)**: 1-3 클리어 → 펑거스 outro(보상팝업 제거) → **전투결과 팝업**(코인·전리품, [확인]만) → [확인] 시 **씬전환 없이**(`hud.inscene_event_requested` → `game._on_inscene_event`) 전투 씬 그대로 이어서: ① 치즈가 **전투 끝난 그 자리에서** 궤짝(`CRATE_X_FRAC=0.25`*vp, 배경 좌측 ~25%)까지 **자동 도보**(`player.event_walk_to`, 궤짝보다 오른쪽이면 **왼쪽 flip 허용** — 인게임 이벤트 한정) ② 치즈+궤짝 **카메라 줌인**(Camera2D zoom→2.3) ③ **컷씬 플레이스홀더**(풀스크린 패널 “🎬 보안관 변신” + 탭) ④ 진행 저장 → 홈. `INSCENE_EVENT_STAGES=[3]`. **(v0.0.69 버그픽스: 일시정지 중 Tween이 안 돌아 줌→컷씬이 행 → 줌을 수동 lerp로 교체. 플레이스홀더 정상 표시.)** 헤드리스 검증(도보 도착·왼쪽flip·walk애니·줌2.3·플레이스홀더 표시, 전 경로 무에러). *(실제 변신 컷씬 아트는 추후 플레이스홀더 교체.)*
- **✅ 이벤트 대본 반영 + 치즈 이벤트 아이들·펑거스 표정(2026-06-04, v0.0.67)**: ① **치즈가 인게임 이벤트/팝업 중 정지 안 함** — `player.set_event_idle(on)`(PROCESS_MODE_ALWAYS + 전투 정지)로 트리 일시정지에도 **1초 유지→idle 1회 재생→반복** 사이클 유지. stage_intro의 3개 일시정지 지점(인트로·팝업·outro)에서 토글. ② **1-1 인트로 `?`→펑거스 정체 공개**: “흐흐… 드디어 돌아왔다”·“웬 고양이가” 2비트 이름표 `?`(`anim:idle/fear`) → 발길질 회상 대사부터 `펑거스`(`angry`). ③ **펑거스 표정 비트별**(`anim` 필드 → NPC 스프라이트 + 초상화 얼굴 동시): 등장=idle/걷기, 당황=fear, 비웃음=laugh, 분노=angry, 퇴장=run. 1-1·1-3 전 비트 대본대로 매핑. 헤드리스 검증(이벤트 중 치즈 idle 재생·종료 후 복귀).
- **✅ [구현 완료] 직업 합성 승급 모델(2026-06-04)**: `craft_grade`가 **직전 등급 소모**(`_remove_grade`) + **2등급 합성 재료**(`GRADE_CRAFT`: 부보안관=검은쥐털10+단추3+옷핀2 / 초급메이드=…포대3+실패2 / 연주자=…바퀴3+못2 / 각 +코인1,000) + **보유=제작잠금**·재제작 가능. 소모된 등급을 장착 중이었으면 새 등급 자동 장착. shop 승급 패널 = 재료 요구·"직전 등급 소모" 표시. 헤드리스 스모크로 검증(owned [1]→[2]·재료차감·코인차감). 3~5등급은 코인만(재료 2막 TBD), **1-19 해금 게이트는 미강제**(상점 상시노출, DEV). 세이브 마이그레이션 불요(`owned_grades` 집합 그대로). 2등급 카드 스탯(직업도감) = base×1.5 코드값과 일치.
- **★자산관리·전투준비 설계 + 스킬/동료 정합(2026-06-04)**: `기획_자산관리_전투준비.md` 신규(직업/스킬/동료 소유·장착 모델 + 전투준비 4탭·마지막세팅). **스킬 3번째 판매 = 1-17** 전 문서 통일(시스템밸런스·홈로드맵·직업도감·자산관리·브리프 일치, 옛 "2막 라쿤" 폐기) — 1막 슬롯 2개라 3개 중 2개 장착. **동료 = 비둘기 1-13 / 치와와 1-16**(치와와가 더 세서 뒤, 코드 `COMPANIONS` meet와 일치). 스킬 귀속=패밀리(코드 `equipped_skills`와 일치). **[밸런스 재계산: 1막 코인 소비곡선]**(3번째 스킬이 1막에 판매되면서 코인 싱크 +).
- **★도감 3종 + 아이템 정책 정리(2026-06-03)**: `기획_직업도감.md`·`기획_침입자도감.md` 신규 + `기획_아이템도감.md` 개편. 침입자 드랍 = **부위+연상 전리품 2종**(전리품 9종 신규). 아이템 분류 **4갈래(전리품/보석/소모품/장난감)**, "가젯"→"장난감". 보석 **병뚜껑 폐기**(→전리품), 사다리 자갈부터 **18종**, **다이아=50,000코인/펄+200**(옛 600·+100 폐기). 벌 드랍=**벌꿀(`honey_drop`)**. 적 표기 전면 **「침입자」**.
- **✅ 드랍/보석/제작 코드 반영 완료(2026-06-03, 옛 [불일치] 해소)**: ① `DROP_TABLE` = 침입자도감 최종표(쥐=털30%+장비15%+잡템10% 3종 / 비-쥐=부위15%+잡템10% 2종, 처치당 독립 굴림). ② `MATERIALS`에 신규 전리품 9종(치즈 부스러기·조각·녹슨 못·단추·실패·옷핀·솜뭉치·빵 조각·벌집) + 보석 18종(자갈~다이아, 새 매입가). ③ `GEM_DROP` = 등급별 독립 드랍(옛 "2%후 비중" 폐기), `GEM_FAVOR` 18종(다이아 +200). ④ 제작: 메이드=털12+포대2+치즈부스러기4 / 음악가=털12+바퀴2+녹슨못1. ⑤ `pearl.gd` 헌납 목록 18종화(`GEM_ORDER`).
- **✅ [밸런스 재계산 해소(2026-06-04, 기획 챗 확정값 반영)]**: ① **펄 호감도 곡선** = 누적 **40/120/320/800**(가속, 코드 `FAVOR_THRESHOLDS` 교체. Lv3=1막 현실 천장). ② **신규 전리품 9종 매입가** 확정(1: 치즈부스러기·솜뭉치·빵 / 2: 녹슨못·단추·옷핀·실패 / 4: 검은쥐털·벌꿀·벌집·치즈조각) — 코드 `MATERIALS` 치즈조각 3→4만 정정(나머지 일치). ③ **1막 경제 재검산**(문서만) = 수입 ≈ 지출 3,500~4,500 ≈ 3,750(한 직업 풀빌드 빠듯, 두 직업이면 파밍 유인). 연동 문서(시스템밸런스 §5.5·§5.2-C·§5.3 / 전리품보석도감 §2·§4) 전부 정합.
- **★날씨/연출 시스템(2026-06-03)**: `weather.gd`를 3-레이어로 분리 — z49 알갱이(근경 아래)·z51 색감틴트(근경 위)·z52 빛(색감 위). 입자=비/눈/낙엽/먼지(갈·회 알갱이)·빛=반딧불/번개(천둥 선행→섬광). 색감=쾌청(라디얼 텍스처 가우시안 태양+좌하단 컬러 렌즈플레어/도넛, 솔리드 코어=스킬슬롯 38px)·노을(태양+주황 그라데이션)·밤(네이비→하늘색)·**흐림(overcast)**. 복합=transition(밤+비→여명→아침)·**shower(흐림+비→마지막 웨이브 진입 시 비→태양 극적 전환, 스포너 wave_started 구독)**. **게임 기본=날씨 없음**(`WEATHER={}`, 1-3만 shower), 테스트는 키 추가. 하프톤=스테이지별 **고정 분위기**(1-1~1-20: 잔잔/아지랑이/바람/안개, 1-10·1-20=폭풍) + 점 최대크기 절반(dot_max 0.22).
- **★스테이지 이벤트(인게임 연출, 2026-06-03)**: `stage_intro.gd`(+main.tscn `StageIntro`). ① **인트로**: 웨이브 전 NPC가 화면 우측에서 치즈 대칭 위치까지 **걸어와** 하단 대화창(얼굴·이름·대사·선택지). ② **팝업**: 트리거 시 **일시정지+화면 Dim+대화창**(예: 첫 처치→펑거스 도발). 대화 중 `get_tree().paused`로 조작 실제 차단(본 노드/UI=PROCESS_MODE_ALWAYS), 화면 어디든 탭=다음. HUD 숨김/복귀·스포너 hold/release. `GameState.enemy_killed` 신호 추가. v1=1-1 펑거스(회색쥐 대역) 인트로+첫처치 도발. EVENTS 딕셔너리로 스테이지 확장.
- **★사운드 전면 도입(2026-06-03)**: 효과음(`Sfx` 오토로드 — Kenney CC0+Pixabay 카툰음+절차적 합성) / **BGM(`Music` 오토로드, 화면별)** — 메뉴=Going Bananas·전투=Drunken Party·**보스(1-10/1-20)=Boss Battle**·프롤로그=자체곡. 홈[설정]=음소거. 근접=카툰펀치(휘두름당 1번·휘두름음 분리)·원거리/근접 타격음 통일·크리=퍼벅·**음악가만 트럼펫(음표 음가별 음높이)**. 컷씬용 BGM 23곡 라이브러리(`assets/music/library/`, git제외·restore.sh). 전부 CC0/로열티프리.
- **★프롤로그 컷씬(2026-06-03)**: `prologue.tscn` — 빗속 상실→거둬짐→발길질→영감 경고 6장면(임시 비주얼+슬픔→온기 음악). 첫 실행 자동 + 시작화면 [이야기] 다시보기(`prologue_seen` 세이브).
- **★배경 3중 레이어 + 절차적 하프톤(2026-06-03)**: far(원경)·ground(지면)·near(근경) — **테마별 랜덤 풀**(한 판마다 다른 배경, 직전 판과 안 겹침·세션 한정). 지면=마젠타 누끼 담장 풀(g01~21)+고정(1-3/1-10/1-20), **발선을 길 구간 중앙에 자동 정렬(기기 무관)**. 원경=château 풀+고정(1-10/1-20=Far/1-20), 매 판 위로 150~400 랜덤(보스 고정 200, 쇠창살 빈틈 방지 클램프). 근경=코너 식물 0~2(서로 다른 코너, 바람 살랑). **AM 하프톤 셰이더**(`halftone_bg.gdshader`, far↔ground 사이)=흰 망점이 도메인 워프로 안개처럼 흐름, 스테이지마다 모션/속도 랜덤, **폭풍 스테이지(1-20)** 강풍 프리셋.
- **문서 정합성**: 로드맵·시스템밸런스의 옛 수치(직업/레벨업/스킬/소모품 가격, 동료 1-13/1-16 순서, 1막 수입)를 §5.2~§5.5·§6-A 최신값으로 통일(2026-06-02).
- **전투준비 = 장비창 + 픽커 (2026-06-03 v2, design.md 토큰 준수)**: `select` 재작성. 기본=장착 치즈+장비슬롯(직업/동료호루라기/스킬/소지품)+스탯+전투력+[출격]. 슬롯 탭→인벤토리 픽커 오버레이(보유목록+선택 설명+[장착]). 3분할 폐기. EDGE/GAP/BAR_H/RADIUS_CARD·FS스케일·컨테이너 정렬 적용. 워딩 '등급 없음'·정식 스탯명. (직업=등급별 장비 모델 위) 헤드리스 로드/장착 검증.
- **★성장 모델(코드 현황, 2026-06-03 — 단 설계는 06-04 '합성 승급'으로 개정, 위 [코드-기획 불일치] 참조)**: "직업 레벨업" **폐기**. 코드 현황 = 각 등급 **별개 제작 장비**(따로 제작·동시 보유). 전투준비 = **장비 장착(로드아웃) + 인벤토리 목록 + 상세설명**. 기획문서 정렬 완료(시스템밸런스 §2.2 콜아웃·§5.2 / 홈로드맵 §4). ✅ **코드 리팩터 완료(2026-06-03)**: `owned_grades`(직업×등급 보유)+`equipped_grade`, `craft_grade`(상위 등급 *별개* 제작, 1등급 선행 필요), 전투준비 로드아웃, 옛 `job_level` 세이브 자동 마이그레이션. maid/jazz 1등급=상점 제작 / 보안관 1등급=1-3 지급.
- **이펙트 전투 연결(2026-06-03)**: `Fx.burst(anim,pos,...)` 헬퍼(9프레임 1회/루프 재생·자동제거). 연결: 적 피격=impact_flash·크리=critical_hit·강넉백=knockback·사망=poof_explosion·스턴=dizzy_stars(루프)·둔화=slime_drip(루프) / 치즈 회복=red_heart·공버프=shine_burst·방패=sparkle·앵콜=shine·폭죽=poof·독=poison_bubbles(루프)·착지=dust·보안관 총구=muzzle_flash·불발=steam. 미연결: exclamation·light_bulb(대화/레벨업용).
- **이펙트 스프라이트 도입(2026-06-03)**: 9프레임 이펙트 시트 15종 → `assets/fx/<name>/0~8.png`(135장). 3×3 균등분할 + **luminance→alpha**(검정 투명·밝을수록 불투명, 연기/집중선 부드럽게) + 알파 무게중심 중앙정렬(재생 흔들림 제거). 세트: poof_explosion·impact_flash·critical_hit·muzzle_flash·knockback·dust·steam·poison_bubbles·sparkle·dizzy_stars·red_heart·light_bulb·exclamation·shine_burst·slime_drip. AnimatedSprite2D용.
- **아이템 에셋 도감(2026-06-02)**: 나노바나나 아이템 아이콘 **73종** 누끼+네이밍 → `assets/items/{food20,gadgets26,loot27}/`. 역카탈로그 `docs/기획_아이템도감.md`(기존 MATERIALS/CONSUMABLES와 ✅11종 매핑 + 🆕신규 후보). 보석 4종(조약돌/자수정/사파이어/다이아)·bee_sting은 그림 추가 필요.
- **UI 카툰화 2차 — 실제 에셋 도입(2026-06-02)**: 나노바나나 에셋 42장 → 마젠타 제거+크롭 146개(`~/Downloads/UI graphic/cut/`). 게임 적용분: ① 가방 패널 = **빈티지 간판 텍스처**(`Design.frame_signboard`, 9-slice NinePatch) ② HUD 일시정지 = **아이콘 뱃지**(`icon_pause`) ③ **전투 하단바 전체** = 텍스처(근접/원거리=글러브 손 `action_*`, 칸=`slot2_*` 사각/원형 HUD 슬롯). ④ **모든 버튼 = 베벨 알약 텍스처**(`pill2_*` 9-slice, 추가 제공분 — 모서리 평평·별 장식·베벨. 골드=brand/빨강=primary/크림=secondary/회=disabled). 큰 버튼일수록 예쁨(짧은 버튼은 약간 통통). ③ 알약 버튼 텍스처(`btn_pill_*`)는 "납작한 로젠지" 형태라 버튼 비율에서 타원으로 떠서 **버튼엔 미적용**(코드 알약 유지, 에셋은 repo 보관). 9-slice 프레임/아이콘/심볼/손 에셋은 비율 문제 없어 순차 적용 예정.
- **UI 카툰화 1차(2026-06-02)**: design.md 갱신본 적용 시작. ① 토큰 오토로드 `Style`→**`Design`**으로 리네임(SSOT, `scripts/style.gd`) + §9 토큰 전량 반영(간격/크기/STROKE/MOTION). ② 버튼 **베벨 입체**(상단 하이라이트 림 + 하단 음영 림 + 알약 라운드 + 단색 그림자), **CTA 색=의미 분리**(brand=골드 성장/획득, primary=빨강 전투/돌입, secondary=크림 뒤로). ③ 패널 **물건화 헬퍼** `make_panel()`(이중 테두리 + 모서리 볼트4 + 리본 헤더) / `framed_plate()` 추가. ④ **시작화면 적용**: [게임 시작]=골드 베벨 CTA, 버전=금속 명판(볼트). 진단 로드맵 순서대로 다음 화면(홈→상점→전투준비→전투 HUD) 순차 적용 예정.

---

## 1. 한눈에 보기

기획서의 **코어 전투 루프(개발가이드 Phase 1~5)** + 그 위 **메타 루프(홈로드맵 1~6단계: 코인·홈·전투준비·상점·스킬·펄·동료) 전부 구현 완료** + 기획에 없던 **연출·UI(Caldera 디자인)·사운드·웹배포 인프라**까지. **침입자 10종(보스 제외) 진짜 스프라이트 완료**, 별점 시스템·스테이지 맵·로컬 세이브(AUTOSAVE)도 됨. **1막 완성(0.1.0)까지 남은 핵심:** ① 보스 2종(펑거스/큰뱀) 실제 패턴·스프라이트(현재 placeholder 덩치) ② 난이도 M배율 플레이어 적용(현재 개발자만) ③ 이벤트/프롤로그/게임오버 진짜 아트(구조·대본은 됨) ④ 세이브 웹 영속화 검증.

---

## 2. ✅ 구현된 것 (현재 작동)

### 코어 전투
- [x] 가로모드 2D 횡스크롤, 단일 화면 고정
- [x] 치즈 = 살아있는 관문 (적이 닿으면 멈춰 싸움, 통과 불가)
- [x] 좌우 이동(WASD) / 점프(회피) / 앉기(공중 적 음파 회피)
- [x] **근접/원거리 공격 버튼 분리** (수동 2버튼, 거리 자동전환 폐기) + 원거리 자동조준
- [x] **공격 2단계 속도**: 준비동작=빠름(누르는 즉시 발사 느낌) → 발사/타격 후 마무리=자연스럽게
- [x] **공중 적은 원거리로만**(점프해 직선/땅에서 포물선), 근접은 점프해야 공중 적 타격
- [x] 몸으로 밀기 (정지중=누구나 / 전진중=속도 빠를 때 / 최대 3마리)
- [x] 치즈 HP = 실패 게이지, 게임오버
- [x] **크리티컬** — 직업별 효과(보안관 강타 / 메이드 강넉백 / 음악가 스턴)

### 직업 (4종, 전부 Lv1 고정 상태)
- [x] 맨몸(길냥이) / 보안관 / 메이드 / 음악가 — 스탯·스프라이트·근접/원거리 평타
- [x] 시작 직업 선택 화면 + 호버 시 idle 애니 미리보기
- [x] **직업 명칭 + 등급 라벨** 시스템 (데이터틀 완성, 표시 작동)

### 적 (침입자 10종 진짜 스프라이트 완료 / 보스 2종만 placeholder)
- [x] **침입자 10종 진짜 스프라이트** (`enemy.gd` `ENEMY_FRAMES`): 회색3·박쥐·벌·참새·거미 = 실제 프레임(idle/walk/attack/hit), 검은3 = 회색 프레임 셰이더 리스킨. *placeholder 도형은 **보스 2종만**.*
- [x] **적 10종 데이터/행동** (`enemy_data.gd` + 범용 `enemy.gd`): 회색3·검은3·박쥐·참새·벌·거미
- [x] 종류별 스탯·크기·이름표 + 방어력 + 상태이상(독·둔화)
- [x] 근접(찌르기) / 원거리(직선·포물선 탄환) / 공중(띄움)
- [x] 이동 분리(§3.1): 투척쥐·거미=멈춰 발사 / 박쥐·벌=비행하며 발사 / 참새=공격 때 급강하
- [x] **박쥐 = 머리 높이 수평 음파**(§3.1) — 서면 맞고 **앉으면 회피**
- [x] 적끼리 겹침 통과(빠른 적 추월), 치즈 관문엔 막힘 / 피격 경직 타이머
- [x] **캐릭터 크기**: 치즈 원본 100% / 적은 치즈와 균형(원본 ×1.25)
- [x] **보스 placeholder**(§3.3): 펑거스(1-10, HP1000)·큰뱀(1-20, HP1900) 단일 개체·큰 덩치·접촉공격. *패턴·스킬은 미구현*
- [x] **스테이지별 웨이브**(§6-B 1-1~1-19) — 스테이지마다 다른 적 구성. *2026-06-02 개정: 새 적 등장 앞당김(1-2 롤러쥐/1-3 투척쥐) + 거의 모든 웨이브 2종+ 혼합. 기획서 표와 `enemy_data.gd` 동기화.*

### 진행 / 메타 루프
- [x] **홈 화면(허브)** `home.tscn` — 실내 placeholder 배경 + 치즈(대기) + 상단바(코인·스테이지·[지도]) + 펄·맥스·[전투준비]·[출격]. [지도]=**스테이지 맵**(별점 허브, 아래 항목). 흐름: 시작→홈→[출격]전투 / [전투준비]→직업선택→홈 / 클리어→[다음▶] or [홈으로]
- [x] **코인 경제** — 처치 코인·첫클리어 보너스·HUD 표시·세이브 (로드맵 1단계)
- [x] **★ 스테이지 별점 시스템**(시스템밸런스 §5.7) — 클리어 "전투 실시간"으로 ★1~3 판정(일반=웨이브수×30s/20s, 보스=180/120s). **게이팅 없음**(클리어=무조건 진행). **첫 클리어 보상 별 차등**: 코인 base(`20+5×N`+보스보너스)에 ★2 ×1.5/★3 ×2 배수 + ★2 하급보석·★3 중급보석 1개 랜덤(첫클리어 1회 고정). **best_star 재도전 max 갱신** → **구간 올스타**(1-1~10/11~20/전체 전부★3) 각 1회 자수정·로즈쿼츠·사파이어. 전투 HUD 상단 경과시간(일시정지·이벤트 제외) / 클리어 결과창 ★·시간·보석 / 홈 ★N/60 진행도. 세이브 `stage_stars`·`allstar_claimed`(옛 세이브=빈값 마이그레이션). SSOT=`game_state.gd`(`rate_stars`·`award_stage_clear(stars)`·`first_clear_star_gem`·`claim_allstar`). per-stage 별 표시·올스타 수령은 **v0.0.80 스테이지 맵**에서(올스타는 맵 [받기] 수동 수령).
- [x] 스테이지 클리어 → 다음 흐름, 웨이브 시스템, 클리어/게임오버 패널
- [x] **클리어 화면 전리품 표시**(이번 판 드랍 요약) + **해금 이벤트 placeholder**(1-3 보안관/1-5 펄/1-7 상점 등 → [확인] 강제 후 진행, 로드맵 2-B 구조)
- [x] **가방(인벤토리) 오버레이** — 홈 [가방]: 전리품·소모품·보유 스킬 현황(읽기 전용)
- [x] **개발자 메뉴 "스킬/동료 지급+장착(테스트)"** — 켜면 전투 중 스킬칸·동료칸/키로 발동 테스트(맨몸은 보안관 자동 전환). 동료 소환 시 placeholder 비주얼(비둘기 날며 똥/치와와 질주)
- [x] **직업 해금 타임라인(§6-A, 플레이어 모드)**: 1-3 클리어=보안관 / 1-7 클리어=메이드·음악가 (세이브 유지, select 화면 per-job 잠금)
- [x] 상단 스테이지 표시, 테스트 치트(0키=전 직업 해금, DEV)

### 연출/주스 (기획서엔 없던 폴리시)
- [x] 캐릭터 발밑 그림자 (점프=지면 투영, 눕기=프레임 연동), 탄환 그림자
- [x] 떠오르는 데미지 숫자 (적=흰/금색, 치즈=빨강)
- [x] 총구 화염 / 불발 연기 / 화면 흔들림 / 히트스톱

### UI / 비주얼 — **Caldera 디자인 시스템** (신규 도입)
- [x] 라이트 테마(Basalt Canvas), Digital Orange CTA, 둥근 알약 버튼
- [x] 시작 화면(타이틀 아트 + [게임 시작] + 버전)
- [x] HP바 빨강+테두리, 상단 딤 띠, 패널(주요=오렌지/보조=고스트)
- [x] **하단 HUD 단일 행**(흑백 반투명): 좌하단 조이스틱 / 아이템1~3 · 동료 · 스킬1~4 · 근접공격 · 원거리공격 (스킬·아이템·동료는 placeholder)
- [x] 폰트: **Pretendard**(깔끔·중립 한글 산세리프. TTF 임베드 → Godot 자체 래스터화로 웹 무관하게 안 깨짐)

### 🔊 사운드 / 음악 (2026-06-03 신규)
- [x] **효과음** `Sfx`(오토로드) — 클릭·코인·타격(punch)·크리(퍼벅)·휘두름·처치·점프·회복·버프·클리어. Kenney CC0 + Pixabay 카툰음 + 절차적 합성(불발 철컥·트럼펫). 근접=카툰펀치(휘두름당 1번·0.05s 지연·휘두름음 분리), 근접/원거리 타격음 통일, **음악가만 트럼펫(음표 음가별 음높이)**.
- [x] **BGM** `Music`(오토로드, 화면별 자동전환) — 메뉴=Going Bananas / 전투=Drunken Party / **보스(1-10·1-20)=Boss Battle** / 프롤로그=자체곡. 끊김 없는 루프·-14dB. **홈 [설정]=음소거 토글**(저장). 전부 FreePD/Pixabay CC0.
- [x] **컷씬용 BGM 라이브러리** 23곡(`assets/music/library/`, `.gdignore`+git제외, `restore.sh`로 복원) — 슬픔/펄로맨스/보스/쉐도우/엔딩 장면별 분류(`MANIFEST.md`).

### 🎬 서사 / 컷씬 (2026-06-03 신규)
- [x] **프롤로그 컷씬** `prologue.tscn` — 빗속 상실→거둬짐→발길질→영감 경고 6장면(임시 비주얼: 빗줄기·실루엣·만화기호 + 음악 슬픔→온기). 탭 진행·[건너뛰기]=전체 스킵(반응형). 첫 실행 자동 + 시작화면 [이야기] 재생(`prologue_seen`).

### 🖼️ 배경 시스템 — 3중 레이어 + 절차적 하프톤 (2026-06-03 신규)
- [x] **3레이어**: far(원경, 가운데·매판 위로 150~400 랜덤) → **AM 하프톤 셰이더** → ground(지면, 하단) → [캐릭터] → near(근경, 캐릭터 앞). `stage_background`/`ground_layer`/`foreground`.
- [x] **테마별 랜덤 풀**(`POOL_COUNT`) — 한 판마다 far/ground/near 랜덤. **직전 판과 안 겹침**(static, 세션 한정). 1막=`wall` 테마(원경12·지면21·근경 코너별7).
- [x] **고정 스테이지**: 지면 `FIXED_GROUND`(1-3/1-10/1-20), 원경 `FIXED_FAR`(1-10·1-20=Far/1-20, 높이도 고정 200).
- [x] **지면 발선 자동 정렬** — 이미지 '서는 면'을 `ground_y`(기기마다 계산)에 맞춤 → 기기 무관. 길 있는 지면은 길 구간 중앙에(크롭 상한).
- [x] **근경 코너 식물** 0~2개(서로 다른 코너, 한 코너 중복 금지) — 마젠타 누끼 후 4코너 분리(코너별 7조각). 모서리에 고정 + **바람 살랑**(뿌리 기준 회전).
- [x] **AM 하프톤 셰이더** `halftone_bg.gdshader` — 흰 망점이 그리드 고정·반지름=흐르는 fbm 밝기, **도메인 워프로 안개처럼 소용돌이**. uniform(밀도/크기/속도/대비/알파…). **스테이지마다 모션·속도 랜덤**, **폭풍 스테이지(1-20)** 강풍 프리셋. 원경 안개로 시작해 망점→셰이더로 발전(옛 코드-안개는 off).
- [x] **마젠타 누끼 에셋 파이프라인** — 원경(château)·지면(담장)·근경(식물) 전부 마젠타 키아웃(PIL/numpy).

### 인프라 / 웹배포
- [x] GitHub Pages 자동배포 (push → 약 1~2분)
- [x] OG 썸네일(링크 미리보기) + 고양이 파비콘
- [x] **웹 한글 깨짐 완전 해결** (모든 텍스트 폰트 직접 지정)
- [x] 캐시버스트 (파일명에 커밋해시 → 새로고침만 해도 최신)
- [x] PWA / 첫 터치 풀스크린 (모바일 주소표시줄 숨김; iOS는 "홈 화면에 추가")

### 🛠️ 개발 인프라 — 플레이어/개발자 모드 분리 (설계: `설계_플레이어-개발자모드.md`)
- [x] **DEV 게이트** (`GameState.is_dev()`) — 출시 빌드 만들 때 false면 dev UI 전부 사라짐
- [x] **런 설정** (mode/difficulty/cheats/coins) — 게임 본체가 이것만 읽어 실행
- [x] **개발자 메뉴 씬** (`dev_menu`) — 직업/등급(Lv1~5)/스테이지/난이도(M)/치트(무적·적즉사·적수) + 스킬·아이템 빈 슬롯
- [x] **시작화면 개발자 모드 진입** = 우하단 작은 흰 동그라미(나만 인지, 호버 없음) / [게임 시작]=플레이어 모드
- [x] **인게임 디버그 오버레이** 🐞 (**dev 모드 전용** — 플레이어 모드엔 안 뜸) — **2칼럼**(왼:치트·스테이지점프 / 오른:직업변경·적10종 스폰)
- [x] **개발자 모드도 클리어 시 다음 스테이지로 이어짐** (1-5→1-6, 설정 유지)
- [x] **테스트 스테이지(샌드박스)** — 자동 웨이브 없이 🐞로 직접 적 스폰해 직업×적 상성 테스트
- [x] **진행 저장/이어하기** (`user://save.json`) — `GameState.AUTOSAVE` 토글 (**개발 중 false** = 항상 1-1부터 / 출시 땐 true)
- [x] **Lv 배율** (시스템밸런스 §2.2: Lv1~5 = 1.0/1.5/2.2/3.2/4.5) — hp/원/근에 적용
- [x] **난이도 M 배율** — 적 HP·DMG에 곱함 (개발자 메뉴서 조절)

---

## 3. ⚖️ 기획서와 달라진 점 (의도적 변경)

### 직업 스탯 — "더 극단적으로" 튜닝 (기획서 §4 → 현재)
| 직업 | 체력 | 원거리 | 근거리 | 공속 | 이속 | 크리율 |
|---|---|---|---|---|---|---|
| 맨몸 (기획 100/8/12/1.0/1.0) | **80** | **6** | **8** | **0.9** | 1.0 | 5% |
| 보안관 (기획 130/11/10/1.1/1.0) | **140** | **13** | **11** | **0.8** | 1.0 | 15% |
| 메이드 (기획 120/6/16/1.0/1.15) | **110** | **5** | **18** | 1.0 | **1.25** | 15% |
| 음악가 (기획 110/14/6/1.0/1.0) | **90** | **16** | **4** | **0.9** | **0.9** | 12% |

- 음악가 체력 90 — 기획 원칙(간판 110~130)보다 낮음. 화력 최강의 대가로 물몸.
- **음악가 공속 0.7→0.9(2026-06-03)** — 원거리 평타가 과하게 느려서 맨몸(0.9)과 동일하게.
- 근접 사거리 **300**(340→300, 2026-06-03 약간 단축). 근접 한 번에 때리는 **최대 타겟 = 직업별**(`JOB_STATS.melee_targets`): 맨몸·보안관·음악가 **2** / **메이드 3**. *(밀기 마릿수 한계 3은 별개)*
- 🎺 **음악가 원거리 — 음표=음가별 탄속/소리(2026-06-03)**: 잇단음표=보안관 총알속도(840)·8분=540·4분=느림(230). 빠른 음표일수록 위아래 물결 작고 완만(덜 방정맞게). 소리도 음악가만 **트럼펫**(음가별 음높이).

### 공격 — 근접/원거리 **버튼 분리**(자동전환 폐기) + 직업별 원거리 방식
- **근접 버튼 / 원거리 버튼 따로** (목업 기준). 거리 자동판정 제거 → 버튼이 공격 종류 결정. (키보드 K=근접 / L=원거리)
- **새 원칙**: "원거리 공격력 셀수록 사거리 짧다" (기획엔 없음)
- 🎺 **음악가**: 음표(♪) 탄환, 느림 + 위아래 물결 + **화면 0.72(≈¾) 사거리**(끝에서 페이드, 투명하면 데미지0). 탄마다 모양/속도/물결 랜덤. *(2026-06-02 0.5→0.72로 사거리 상향 — 너무 약해서)*
- 🤠 **보안관**: 직선 총알 끝까지 + **불발 12%**(연기+철컥음)
- 🧹🐾 **메이드·맨몸**: **랜덤 각도 포물선 손던지기**(위로도) → 공중 적 명중 가능. 메이드=접시(큰 타원), 맨몸=돌(회색). 바닥에 닿으면 **깨짐**.
- 발사 딜레이(준비동작 빠른 속도에 맞춰 단축: 돌·접시 0.2s / 총 0.12s / 음표 0s)
- **이동 중 공격 불가** (공격하면 멈춤, 새로 밀면 공격 취소)

### 표기/명칭 (기획 §0 반영, 데이터화)
- 직업 호칭(Lv별) — 보안관: 견습→부→보안관→주→보안관장 / 메이드: 견습→초급→중급→고급→시녀장 / 음악가: 거리악사→연주자→악장→명연주자→마에스트로 / 맨몸: 길냥이(등급 없음)
- 등급 라벨(공통): 이름없는→떠오르는→소문난→이름난→전설의 (Lv별 색)

---

## 4. ⏳ 앞으로 할 것 (기획엔 있는데 미구현)

> **★ 0.1.0 (1막 완전 완성) 남은 체크리스트** — 시스템(로드맵 1~6단계)은 완료. 남은 건:
> - [ ] **🅰 보스 2종 실전화** — 펑거스(1-10)·큰뱀(1-20) 패턴(돌진/장판/2페이즈) + 실제 스프라이트. *(+ 보스 컷씬 대본 1-10/1-20 = 기획 챗 확정 필요)*
> - [ ] **🅱 실제 아트 갈아끼우기(컷씬·GUI)** — 펄/맥스 캐릭터·홈 실내 배경·이벤트 캐릭터(현재 동그라미)·각 이벤트 컷씬·프롤로그·게임오버 = 전부 placeholder → 진짜 그림. *구조·대본·연출 비트는 이미 됨, 그림만 끼우면 됨.*
> - [ ] **🅲 세이브 웹 영속화 검증** (작음) — 배포 후 새로고침으로 진행 유지 확인.
> - [⏸] **M배율** — 적용 여부는 밸런스 테스트로 결정(보류).
> - [후순위/결정필요] **F. 애착 레이어** — 펄 로맨스 5단계 컷신·홈 인터랙션(다마고치)·게임오버 서사 운율. 기획상 "콘텐츠 단계"라 **0.1.0에 넣을지 vs 출시 후로 뺄지 결정** 필요.
> → **🅰+🅱+🅲 = 1막 "플레이 완성". F까지 = 1막 "정서 완성".**

> 우선순위는 같이 정하면 됨. 카테고리별 정리.

### A. 콘텐츠 다양성 (체감 큰 것)
- [x] **침입자 10종 진짜 스프라이트 완료**(§2 참조 — 회색3·박쥐·벌·참새·거미 실제 프레임 + 검은3 리스킨). 남은 것은 밸런스 튜닝 정도(행동·타이밍은 폴리시 완료).
- [~] **보스 2종**: 펑거스(1-10)·큰뱀(1-20) — **placeholder 구현**(큰 덩치+큰 피통+접촉공격, `enemy_data.gd` `sprite:false`). 남은 것: **진짜 스프라이트**(펑거스는 NPC용 `fungus_frames.tres` 있음→전투 연결 가능 / 큰뱀은 그림 필요) + **돌진/장판/2페이즈 패턴**(§3.3). ← *1막 완성 최대 구멍*
- [⏸] **난이도 M배율 — 적용 보류(밸런스 테스트로 결정)**. §6-B 웨이브 1-1~1-19 구현. M배율·마릿수 배율은 **개발자 메뉴 슬라이더로 이미 실험 가능**(난이도 0.5~3.0 / 침입자 수 1.0~5.0). "플레이어 모드에 자동 곡선으로 박을지 vs M=1.0 고정"은 실플레이 밸런스 테스트 후 확정 → 그때 코드 반영.

### B. 성장 시스템 (게임 핵심 루프)
- [x] **등급 시스템 = 합성 승급(직전 등급 소모)** — `craft_grade`가 **직전 등급(`_remove_grade(ng-1)`) + 재료 + 코인 → 다음 등급** 모델 구현(2026-06-04 설계와 일치, 등급당 0/1개·`owned_grades` 중복방지). 전투준비 장비 장착·세이브 마이그레이션 완료. 장착 등급이 배율(§2.2)·스킬슬롯수 결정. 비용 §5.2-C. *(2등급 합성 재료만 정의 `GRADE_CRAFT`, 3~5등급 재료는 2막 TBD=현재 코인만 — 의도된 미정.)*
- [x] **스킬 구매·장착·발동** — **구현(로컬)**: 9종(직업×3, §4.2 수치). 맥스 [스킬] 탭 구매(직업귀속·order≤2, 3번째=2막) → 전투준비 [스킬] 탭 슬롯 장착(직업×Lv: 맨몸0/Lv1·2 2/Lv3·4 3/Lv5 4) → 전투 중 스킬칸·키 발동(쿨타임·placeholder 플래시). 효과: 멈칫/넉백/장판둔화/스턴/광역딜/지원60×Lv/방패40%/앵콜. Lv배율은 수치만(§규칙C). *(로드맵 5단계)*
- [x] **상점(맥스)** — **구현(로컬)**: 탭 구조(레벨업/소모품/전리품 매입/직업 제작). `scenes/shop.tscn`. *(로드맵 4단계)*
- [x] **전리품 & 드랍** — **구현(로컬)**: 적 처치 시 드랍(쥐 티어별 털30%+행동별 장비15% / 비-쥐 부위15% / 보석2% 별도굴림, 종류비중 §5.5). `materials` 인벤토리·세이브. 맥스 매입(§5.2-C).
- [x] **직업 제작(메이드·음악가)** — **구현(로컬)**: 전리품+코인500(§5.2-B), 상점 제작 탭. *해금=제작으로 전환.*
- [x] **코인 경제** — 획득·표시·저장·**소비(상점)** 모두 구현(로컬). *(로드맵 1·4단계)*
- [x] **소모품 실제 작동** — **구현(로컬)**: 낡은 붕대(회복30)/말린 멸치(공격+50% 8s)/폭죽(광역60). 상점 구매→전투준비 슬롯 장착→전투 중 아이템칸/키1·2·3 발동, 재고 차감(§5.4).

### C. 관계·동료 (정서 레이어)
- [x] **펄(축복+호감도)** — **구현(로컬)**: 홈 [펄] → **미연시풍 대화 화면**(`pearl.tscn`: 캐릭터 placeholder 표정 + 하단 대화창 + 선택지). 출격 축복 택1(발톱/배/곳간, 호감도 Lv로 15→30%·25→50%) = **1회용**(매 출격마다 다시 받음, 전투 시작 시 소비) + 보석 헌납→호감도(30/80/200/500). 컷신은 호감도Lv별 placeholder 대사. *(로드맵 6단계)*
- [x] **NPC 대화 화면 베이스** (`npc_talk.gd`+`npc_figure.gd`) — 캐릭터 placeholder(고양이풍 도형·표정) + 하단 대화창 + 선택지. 펄·맥스(`maxtalk.tscn`: 인사→상점)가 사용. 진짜 아트 나오면 figure만 교체.
- [x] **호루라기 동료** — **구현(로컬)**: 비둘기(광역 둔화40%3s+딜)·치와와(지상 적 우측 강제밀기). 맥스 [동료] 호루라기 구매 → 전투준비 [동료] 슬롯1 장착 → 전투 중 동료칸/키4 호출(쿨타임). *(로드맵 6단계)*

### D. 기타
- [x] **홈 화면 / 직업·장비 변경** — 홈 허브(§2)·전투준비(select 4탭) 완성. 남은 건 펄/맥스 최종 아트·홈 인터랙션(F).
- [x] **사운드/음악** — 효과음(클릭·코인·타격·크리·펀치·휘두름·처치·점프·회복·버프·클리어, Kenney CC0+Pixabay) + **BGM 루프**(카툰, 설정서 음소거). 남은 것: 스킬·동료·이벤트 전용음, 화면별 BGM 다양화

### E. 출시 선행 과제 — BM 작동 전 필요 (`기획_브리프.md` §10-6)
> 신규 기획(2026-06-02): 수익모델 갱신본 §10. **다운로드가 수익의 유일한 진짜 레버** / P2W·가챠·동전 현금판매 금지 / 애정(펄·친밀도)은 무료, 코스튬·후원 팁으로 수익.
- [x] **세이브/진행도 (로컬)** — v0.0.86부터 **AUTOSAVE 켜짐**(`user://save.json`, 부팅 시 로드·[이어하기]·[처음부터]). *웹(IndexedDB) 영속화는 배포 후 새로고침 검증 필요.* 클라우드(iCloud/Play Games)는 출시 후속.
- [ ] 인앱결제(IAP) 연동(애플/구글) + 소상공인(수수료 15%)
- [ ] 광고 SDK(미디에이션) — 리워드 우선(부활·동전2배·무료상자), 전면광고는 신중
- [ ] iOS ATT·광고 동의(프라이버시) 처리
- [ ] 나라별 가격 현지화 테이블 / 다국어 풀 장착 + 단계적 출시(§10-5)
- [ ] 코스튬(성능 분리)·후원 팁·스타터 팩 상품화

### F. 서사 연출 반영 포인트 — 신규 기획 (`기획_세계관_치즈서사.md` §6, `기획_펄로맨스.md`, `기획_홈인터랙션.md`)
> 전부 미래 청사진(미구현). 1차 출시엔 프롤로그 '씨앗 컷'만, 본격 연출은 콘텐츠 단계.
- [~] **프롤로그 컷신** — **임시 비주얼로 구현됨**(`prologue.gd`, 6장면, 빗속 상실→거둬짐→발길질→영감 경고, 슬픔→온기 음악). 남은 것: 진짜 아트로 교체, 사일런트 연출 강화(자막 줄이기), 쉐도우 씨앗 디테일.
- [ ] **게임오버 연출 = "빗속으로 다시 내던져짐"** (프롤로그 상실과 비 모티프로 운율)
- [ ] **홈 허브/다마고치** — 영원한 이방인 디테일(문 옆 박스 잠 등) + 특이점(달 보며 욺 등) *종종* 등장, 막 진행·펄 호감도로 **옅어지는 상태 플래그**. → 홈 인터랙션 "기분/상태" 모델에 얹기(별도 시스템 X).
- [ ] 특이점은 **코스튬 무간섭**(몸에 거는 슬롯 금지, 연출/애니로만)
- [ ] 홈 인터랙션(치즈 탭 랜덤반응·기분·데일리 보상) + 펄 로맨스(호감도 5단계 컷신·선택대화 점수형) — 애착 엔진

> ※ 세이브/진행도 = v0.0.86부터 **AUTOSAVE 켜짐**(플레이어 모드 자동 저장·부팅 이어하기). 개발자 모드는 저장 안 함(테스트 격리). 웹 영속화 배포 검증만 남음.

---

## 5. 📁 코드 위치 메모 (개발 참고)
- 직업 스탯/명칭/등급: `scripts/game_state.gd`
- 플레이어(이동/공격/그림자/데미지숫자): `scripts/player.gd`
- 탄환(직업별 발사·모양·깨짐·그림자): `scripts/bullet.gd`
- 적(범용): `scripts/enemy.gd` / 적 정의·웨이브: `scripts/enemy_data.gd`(오토로드 Enemies) / 적 탄환: `scripts/enemy_bullet.gd` / 스포너: `scripts/spawner.gd`
- 개발자 메뉴/디버그 오버레이: `scripts/dev_menu.gd`, `scripts/debug_overlay.gd`
- HUD/패널: `scripts/hud.gd` / 하단 조작: `scripts/bottom_hud.gd`, `scripts/joystick.gd`
- 레이아웃(바닥선/조작띠): `scripts/layout.gd`
- 직업 선택/전투준비: `scripts/select.gd` / 시작화면(이어하기·처음부터): `scripts/start.gd`
- 게임 코디네이터(클리어/게임오버/인스씬 이벤트): `scripts/game.gd`
- 홈 허브: `scripts/home.gd` / **스테이지 맵(별점·올스타 수령): `scripts/stagemap.gd`** / 상점: `scripts/shop.gd`
- NPC 대화 베이스: `scripts/npc_talk.gd`·`scripts/npc_figure.gd` / 펄: `scripts/pearl.gd` / 맥스: `scripts/maxtalk.gd`
- 스테이지 이벤트(인트로·팝업·컷씬): `scripts/stage_intro.gd` / 프롤로그: `scripts/prologue.gd`
- 배경 3레이어: `scripts/stage_background.gd`·`ground_layer.gd`·`foreground.gd` / 하프톤·날씨: `scripts/halftone_bg.gd`(+셰이더)
- 디자인 토큰(SSOT): `scripts/style.gd`(오토로드 Design) / 테마: `assets/ui/caldera_theme.tres`
- 사운드: `scripts/sfx.gd`(Sfx) / 음악: `scripts/music.gd`(Music)
- 웹 배포: `.github/workflows/deploy.yml` + `export_presets.cfg`

---

## 6. 🗒️ 버전 히스토리 (요약)
> **`0.0.56`+ (X.Y.Z 체계 전환 이후) 상세 변경은 이 문서 맨 위 ✅ 변경 로그 참조.** 아래는 그 이전(0.NN) + X.Y.Z 마일스톤 한 줄 요약.
- **0.0.86** 진행 영속화(AUTOSAVE ON·부팅 로드·[이어하기]/[처음부터]) · **0.0.85** DEV 도구 개발자 루트 한정 · **0.0.84** 개발자 메뉴 스크롤 접근성 · **0.0.83** 클리어 화면 개편(CLEAR+별+보상박스) · **0.0.81** 스테이지 맵 design.md 정합 · **0.0.80** 스테이지 맵(별점 허브)
- **0.0.79~0.0.77** 별점 시스템(★1~3·별 차등 보상·구간 올스타) · 검은쥐 셰이더(털만)
- **0.0.76~0.0.67** 1막 비-보스 이벤트 전부 + 인스씬 컷씬·게임오버 컷씬 + 통일 귀신 + 침입자 공격 프레임 동기·공중 상하진동 + 1-3/1-5/1-7 해금 이벤트
- **0.0.66~0.0.57** 침입자 7종+펑거스 실제 스프라이트 · 검은쥐 리스킨 · 배경 3레이어+하프톤 · 프롤로그 · BGM 화면별 · 효과음 Kenney
- **0.06** 직업별 원거리(음표/포물선/불발), 이동중 공격 불가
- **0.07** 스테이지 진행(1-1→1-2 해금), 잠긴 직업, 그래픽 퀄업
- **0.08** 그림자, 근접 3마리, 앉기 수정, 선택화면 2배+호버, 시작화면, OG 아이콘
- **0.09** 치즈 피격 데미지 숫자
- **0.10** 직업 명칭+등급 라벨 시스템
- **0.11~0.16** 웹 한글 깨짐 완전 해결(폰트 직접 지정) + 캐시버스트 · Caldera 디자인 시스템
- **0.17** PWA/풀스크린(모바일 주소표시줄 숨김)
- **0.18** 개발자/플레이어 모드 분리 · 진행 저장/이어하기 · Lv 배율 · 난이도 M · 디버그 오버레이(🐞)
- **0.19** 적 10종 placeholder + 스테이지 웨이브(§6-B) · 겹침/이동분리/상태이상 · 개발자 도구 확장 · 직업 해금 타임라인
- **0.20** 스테이지 웨이브 표 개정(§6-B) — 기획·코드 동기화
- **0.21** 근접/원거리 버튼 분리 + 하단 HUD 단일행 · 공격 2단계 속도 · 공중 적=원거리 전담 · 보스 placeholder · 오토세이브 토글
- **0.22** 홈 허브 + 코인 경제 + 클리어 전리품/해금 이벤트 (로드맵 1·2단계)
- **0.23** 맥스 상점(소모품·전리품 매입·직업 제작) + 가방 인벤토리 (로드맵 4단계)
- **0.24** 스킬 시스템 9종(구매·슬롯 장착) (로드맵 5단계)
- **0.25** 펄(출격 축복·보석 헌납 호감도) + 동료(비둘기·치와와) (로드맵 6단계)
- **0.26** 폰트 DoHyeon→Pretendard 전면 교체(웹 안전 TTF)
- **0.27** UI 카툰화 — design.md 토큰(Style→Design)·베벨 버튼·물건화 패널·간판/아이콘 에셋
- **0.28** 이펙트 스프라이트 15종(9프레임) + 전투 이벤트 연결(크리·사망·스턴·독·총구 등)
- **0.29** 아이템 도감 — 아이콘 90여종·보석 19단계·이펙트 누끼/네이밍(assets/items·fx)
- **0.30** 성장 모델 전환(레벨업 폐기 → 등급=별개 제작 장비) + 전투준비 로드아웃(장비창+탭 픽커) 재작성
- **0.31** 여기어때 잘난체(타이틀/버튼) 도입 + 등급=장비 코드 리팩터 마무리(craft_grade·세이브 마이그레이션) + 4·5·6단계 통합 웹 배포
- **0.55** 소모품·장난감 도감 2종 신규(`기획_소모품도감.md`·`기획_장난감도감.md`) + 코드 반영 — CONSUMABLES에 우유75·폭탄150·후추통70·쥐덫90 추가(+type food/toy), 상점 ITEM_ORDER 7종, §5.4 표 확장. 확정 가격원칙=폭죽80 기준선. [구현 대기: 전투 사용·휴대5·쥐덫 설치 메커니즘]
- **0.54** 전리품·보석 인벤토리 설명 도감(`기획_전리품보석도감.md`) 신규(전리품 17 + 보석 18) — 매입가/드랍/호감도 어감 문구집. 도감이 부여한 전리품 매입가 9종을 코드 `MATERIALS` sell에 반영(검은쥐털4·벌꿀4·벌집4·녹슨못2·단추2·옷핀2·치즈부스러기1·솜뭉치1·빵조각1). 보석값·드랍·호감도는 0.53과 일치 확인
- **0.53** 드랍 규칙 재정의 코드 반영 — 쥐=털30+장비15+잡템10·비쥐=부위15+잡템10(독립 굴림) / 신규 전리품 9종 + 보석 18종(등급별 독립 드랍·다이아 50,000c/+200) / 제작 레시피 변경(메이드 털12+포대2+부스러기4, 음악가 털12+바퀴2+못1) / pearl 헌납 18종화. [밸런스 재계산: 호감도 곡선·코인 수입·잡템 매입가]
- **0.52** 도감 3종 정리(직업/침입자/아이템) — 침입자 드랍=부위+전리품2종(전리품9종 신규)·아이템 4갈래(전리품/보석/소모품/**장난감**)·보석 병뚜껑 폐기→전리품/18종(자갈~)·다이아 50,000c·펄+200·벌 드랍=벌꿀(honey_drop)·적 표기 전면 「침입자」. [코드-기획 불일치: 보석값·침입자 드랍] STATUS 기록(밸런스 재계산 별도). **아이템 종류 워딩 "재료"·"잡템" → 「전리품」 공식 통일**(전 문서·코드 유저노출)
- **0.51** ★날씨 3-레이어(알갱이z49/색감z51/빛z52) — 가우시안 태양·컬러 렌즈플레어/도넛·노을·밤·흐림·소나기(1-3 흐림+비→웨이브3에 비→태양) / 하프톤 스테이지별 고정 분위기·점크기 절반 / **스테이지 이벤트 시스템**(인트로 도보 등장 대화 + 일시정지·Dim 팝업) — 1-1 펑거스 인트로+첫처치 도발(`enemy_killed` 신호)
- **0.50** ★전투 튜닝 + 문서 전면 최신화: 음악가 공속 0.7→0.9·트럼펫 음높이↓(모기소리 제거)·메이드 근접 3마리 / idle=첫프레임 1초 유지→1회재생 반복(전 직업) / 음악가 트럼펫음(음가별 음높이) / 근접 사거리 340→300·최대타겟 직업별(기본2·메이드3) / 조이스틱=전투 슬롯 텍스처 톤 / 개발자 오버레이 전투중 스킬·동료 지급
- **0.49** **배경 3중 레이어 + AM 하프톤 셰이더 완성**: far/ground/near 테마 랜덤풀(직전판 비반복)·지면 발선 자동정렬·고정스테이지(far/ground)·근경 코너식물 바람·하프톤(도메인워프 흐름·스테이지 랜덤·폭풍 1-20)·보스 BGM(1-10/1-20)
- **0.48** 배경 시스템 토대: 마젠타 누끼 담장/원경/근경 풀, 2→3레이어, 망점 안개, 원경 올림 랜덤·쇠창살 빈틈 클램프
- **0.47** 근접 사운드 정리(음량↓·타격음 통일·크리 퍼벅·휘두름 분리) + 남은 카툰 효과음(처치/점프/클리어) + 컷씬용 BGM 23곡 라이브러리
- **0.46** 타격음(hit) **추가 증폭** — 소프트리미터(드라이브4)로 체감음량 +6dB(rms -13→-8dB). 피크 정규화만으론 짧은 충격음이 여전히 작아서
- **0.45** 타격음(hit) 1차 정규화(피크 -1.5dB)
- **0.44** 맨몸 고유기 '냥냥펀치' **폐기**(코드 안내문구·기획 시스템밸런스§4.1·STATUS에서 제거) — 맨몸=평타만 튜토리얼
- **0.43** **프롤로그 컷씬** 신규(`prologue.tscn`): 빗속 상실→거둬짐→쥐 발길질→영감 경고 6장면(임시 비주얼=빗줄기·실루엣·기호) + 음악 슬픔→온기 + 첫 실행 자동/시작화면 [이야기] 다시보기(`prologue_seen` 세이브)
- **0.42** BGM **화면별 전환**: 전투=술집 피아노(Drunken Party)·그 외(시작/홈/상점)=Going Bananas. `Music`이 현재 씬 보고 자동 전환(둘 다 FreePD CC0, 웹 곡당 ~1.2MB)
- **0.41** (단일 BGM) 'Maple Leaf Rag'로 교체했다가 0.42에서 화면별 2곡 체제로 대체
- **0.40** **배경음악(BGM)** 도입: 카툰 루프(`Music` 오토로드, 씬 넘어도 끊김 없이 반복·-14dB) + 홈 [설정]=**BGM 켜기/끄기** 토글(저장) / 처치·점프=카툰음, 클리어=딩 팡파레
- **0.39** 휘두름(woosh) **3.5배속**으로 재가공(피치 더↑·0.67초 — 무서운 톤→만화 톤)
- **0.37** 근접 타격 = **카툰 펀치**(Pixabay, 무음 컷)로 교체 + **휘두름당 1번만**(다중타격 마리별 중복 제거) / 근접 버튼 누를 때 **휘두르는 소리(woosh)** 추가
- **0.36** 타격음 교체: 딱딱한 나무(chop)→**부드러운 임팩트**(impact_soft, 2종 변주)·크리=**묵직한 펀치**(impact_punch_heavy) — Kenney Impact Sounds CC0
- **0.35** 효과음 실제 음원화: 클릭·코인·타격·크리·회복·버프 = **Kenney CC0**(assets/sfx/, 표기불필요·상업무료) / 처치·점프·사격은 코드음 폴백(직업별 피치 유지)
- **0.34** 효과음 9종(절차적 생성 `Sfx` 오토로드: 클릭·코인·타격·크리·처치·점프·사격·회복·버프)·핵심 이벤트 연결 / **난이도 모드(기본·하드·헬 재도전 파밍)** 설계 기획 반영(시스템밸런스 §3.4, 구현=맵과 함께)
- **0.33** 해금 이벤트 = **이벤트 씬 랜딩**(이벤트 스테이지 클리어→[확인]→`event.tscn`→홈) · 이벤트 19 워딩 등급제작으로
- **0.32** maid/jazz 제작 차단 버그 수정(상점 '제작 노출'과 '보유' 분리 — `can_craft_job`/shop 표시) + 문서 정합(브리프 §8 1-19=2등급 제작·STATUS 버전사 재정렬·⚠️해소)
